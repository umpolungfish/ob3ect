-- IGProtocol scaffold: VINIT → IMSCRIB → FSPLIT → AFWD → EVALT → AREV → EVALF → FFUSE → ENGAGR → CLINK → IFIX → TANCH
-- Class: a dial that carries a kernel-verified proof (the T/F-lane Witness) between its pinched one-line form and its walked-out multi-line form, the same kernel truth held in both as a B state, forcing the choice between an LLM targeting a line count and a deterministic elaboration ladder. With full Frobenius closure and Lean 4 verification scaffold.
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(2, 7)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑚  | identity — self-imscription
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [6] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [7] FFUSE     stoi   := 𐑙               𐑙 → 𐑳  | fuse μ — assembly mode
--   [8] ENGAGR    stoi   := 𐑳               𐑙 → 𐑱  | engage paradox — B-state, both arms
--   [9] CLINK     fid    := 𐑱               𐑳 → 𐑭  | composition — regime coherence
--   [10] IFIX      prot   := 𐑭               𐑱 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def a_dial_that_carries_a_kernel_verified_dce7f0_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def a_dial_that_carries_a_kernel_verified_dce7f0_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def a_dial_that_carries_a_kernel_verified_dce7f0_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def a_dial_that_carries_a_kernel_verified_dce7f0_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def a_dial_that_carries_a_kernel_verified_dce7f0_protocol : IGProtocol a_dial_that_carries_a_kernel_verified_dce7f0_s0 a_dial_that_carries_a_kernel_verified_dce7f0_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct a_dial_that_carries_a_kernel_verified_dce7f0_s7 a_dial_that_carries_a_kernel_verified_dce7f0_s7 = a_dial_that_carries_a_kernel_verified_dce7f0_s7 (idempotent)
  (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l0 a_dial_that_carries_a_kernel_verified_dce7f0_s0 a_dial_that_carries_a_kernel_verified_dce7f0_s1) (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l1 a_dial_that_carries_a_kernel_verified_dce7f0_s1 a_dial_that_carries_a_kernel_verified_dce7f0_s2) (.seq (.prod (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l2 a_dial_that_carries_a_kernel_verified_dce7f0_s2 a_dial_that_carries_a_kernel_verified_dce7f0_s7) (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l2 a_dial_that_carries_a_kernel_verified_dce7f0_s2 a_dial_that_carries_a_kernel_verified_dce7f0_s7)) (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l7 a_dial_that_carries_a_kernel_verified_dce7f0_s7 a_dial_that_carries_a_kernel_verified_dce7f0_s7) (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l7 a_dial_that_carries_a_kernel_verified_dce7f0_s7 a_dial_that_carries_a_kernel_verified_dce7f0_s8) (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l8 a_dial_that_carries_a_kernel_verified_dce7f0_s8 a_dial_that_carries_a_kernel_verified_dce7f0_s9) (.seq (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l9 a_dial_that_carries_a_kernel_verified_dce7f0_s9 a_dial_that_carries_a_kernel_verified_dce7f0_s10) (.arrow a_dial_that_carries_a_kernel_verified_dce7f0_l10 a_dial_that_carries_a_kernel_verified_dce7f0_s10 a_dial_that_carries_a_kernel_verified_dce7f0_s11))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def a_dial_that_carries_a_kernel_verified_dce7f0_true_arm : IGProtocol a_dial_that_carries_a_kernel_verified_dce7f0_s0 a_dial_that_carries_a_kernel_verified_dce7f0_s11 :=
  (a_dial_that_carries_a_kernel_verified_dce7f0_protocol).restrictToEVALT

-- false arm
noncomputable def a_dial_that_carries_a_kernel_verified_dce7f0_false_arm : IGProtocol a_dial_that_carries_a_kernel_verified_dce7f0_s0 a_dial_that_carries_a_kernel_verified_dce7f0_s11 :=
  (a_dial_that_carries_a_kernel_verified_dce7f0_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def a_dial_that_carries_a_kernel_verified_dce7f0_tier : OuroboricityTier := TierFunctor.obj a_dial_that_carries_a_kernel_verified_dce7f0_s0
#eval a_dial_that_carries_a_kernel_verified_dce7f0_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem a_dial_that_carries_a_kernel_verified_dce7f0_frobenius :
    igFrobeniusAlg.mul a_dial_that_carries_a_kernel_verified_dce7f0_s0 a_dial_that_carries_a_kernel_verified_dce7f0_s0 = a_dial_that_carries_a_kernel_verified_dce7f0_s0 :=
  igFrobAlg_self_fusion a_dial_that_carries_a_kernel_verified_dce7f0_s0
