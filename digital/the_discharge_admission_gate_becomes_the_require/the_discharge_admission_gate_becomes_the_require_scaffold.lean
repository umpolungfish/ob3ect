-- IGProtocol scaffold: VINIT → IMSCRIB → CLINK → IFIX → FSPLIT → AFWD → AREV → IMSCRIB → FFUSE → CLINK → FSPLIT → EVALT → IMSCRIB → IFIX → FFUSE → EVALT → TANCH
-- Class: the discharge admission gate becomes the requirement that every one of the 143 overlap identities and the norm identity reduce to a decided true under native_decide in the flat tower and equivalently floor to full precision under the deep-vanish test, rejecting any identity whose vanishing depth merely creeps with precision as a sign that a coordinate was placed in the wrong layer of the tower
-- Fingerprint: sig=(9,4,2,2)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=False | period=17
-- Expected tier: O₀
-- FSPLIT/FFUSE pairs: [(4, 8), (10, 14)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑱  | identity — self-imscription
--   [2] CLINK     fid    := 𐑱               𐑠 → 𐑭  | composition — regime coherence
--   [3] IFIX      prot   := 𐑭               𐑱 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [6] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [8] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [9] CLINK     fid    := 𐑱               𐑙 → 𐑚  | composition — regime coherence
--   [10] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [11] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [12] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [13] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [14] FFUSE     stoi   := 𐑙               𐑙 → ⊙  | fuse μ — assembly mode
--   [15] EVALT     crit   := ⊙               𐑙 → 𐑡  | evaluate-true — criticality gate open
--   [16] TANCH     top    := 𐑡               ⊙ → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_discharge_admission_gate_becomes_634ba1_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_discharge_admission_gate_becomes_634ba1_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l5 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_discharge_admission_gate_becomes_634ba1_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_discharge_admission_gate_becomes_634ba1_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_discharge_admission_gate_becomes_634ba1_protocol : IGProtocol the_discharge_admission_gate_becomes_634ba1_s0 the_discharge_admission_gate_becomes_634ba1_s16 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_discharge_admission_gate_becomes_634ba1_s8 the_discharge_admission_gate_becomes_634ba1_s8 = the_discharge_admission_gate_becomes_634ba1_s8 (idempotent)
  (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l0 the_discharge_admission_gate_becomes_634ba1_s0 the_discharge_admission_gate_becomes_634ba1_s1) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l1 the_discharge_admission_gate_becomes_634ba1_s1 the_discharge_admission_gate_becomes_634ba1_s2) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l2 the_discharge_admission_gate_becomes_634ba1_s2 the_discharge_admission_gate_becomes_634ba1_s3) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l3 the_discharge_admission_gate_becomes_634ba1_s3 the_discharge_admission_gate_becomes_634ba1_s4) (.seq (.prod (.arrow the_discharge_admission_gate_becomes_634ba1_l4 the_discharge_admission_gate_becomes_634ba1_s4 the_discharge_admission_gate_becomes_634ba1_s8) (.arrow the_discharge_admission_gate_becomes_634ba1_l4 the_discharge_admission_gate_becomes_634ba1_s4 the_discharge_admission_gate_becomes_634ba1_s8)) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l8 the_discharge_admission_gate_becomes_634ba1_s8 the_discharge_admission_gate_becomes_634ba1_s8) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l8 the_discharge_admission_gate_becomes_634ba1_s8 the_discharge_admission_gate_becomes_634ba1_s9) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l9 the_discharge_admission_gate_becomes_634ba1_s9 the_discharge_admission_gate_becomes_634ba1_s10) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l10 the_discharge_admission_gate_becomes_634ba1_s10 the_discharge_admission_gate_becomes_634ba1_s11) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l11 the_discharge_admission_gate_becomes_634ba1_s11 the_discharge_admission_gate_becomes_634ba1_s12) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l12 the_discharge_admission_gate_becomes_634ba1_s12 the_discharge_admission_gate_becomes_634ba1_s13) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l13 the_discharge_admission_gate_becomes_634ba1_s13 the_discharge_admission_gate_becomes_634ba1_s14) (.seq (.arrow the_discharge_admission_gate_becomes_634ba1_l14 the_discharge_admission_gate_becomes_634ba1_s14 the_discharge_admission_gate_becomes_634ba1_s15) (.arrow the_discharge_admission_gate_becomes_634ba1_l15 the_discharge_admission_gate_becomes_634ba1_s15 the_discharge_admission_gate_becomes_634ba1_s16))))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_discharge_admission_gate_becomes_634ba1_true_arm : IGProtocol the_discharge_admission_gate_becomes_634ba1_s0 the_discharge_admission_gate_becomes_634ba1_s16 :=
  (the_discharge_admission_gate_becomes_634ba1_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₀.
def the_discharge_admission_gate_becomes_634ba1_tier : OuroboricityTier := TierFunctor.obj the_discharge_admission_gate_becomes_634ba1_s0
#eval the_discharge_admission_gate_becomes_634ba1_tier  -- the Grammar's own verdict on its tier
