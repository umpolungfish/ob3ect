-- IGProtocol scaffold: VINIT → IMSCRIB → IFIX → IFIX → FSPLIT → EVALT → AFWD → CLINK → EVALF → ENGAGR → AREV → FFUSE → TANCH
-- Class: the imscription of the unit phases u_k are deferred in this ob3ect's first deliverable but the tower design must reserve the same fold mechanism as d equals twelve, namely self-reciprocal palindromic minimal polynomials for u_k on the unit circle collapsing through the two-cosine substitution to a half-degree polynomial whose Galois group is solvable or degree at most four, so that when a-zero moduli are pinned the subsequent ob3ect can adjoin phase generators without exiting the magnitude-extended double cover
-- Fingerprint: sig=(6,2,3,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=13
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(4, 11)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑭  | identity — self-imscription
--   [2] IFIX      prot   := 𐑭               𐑠 → 𐑭  | irreversible fixation — winding number
--   [3] IFIX      prot   := 𐑭               𐑭 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [7] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [8] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [9] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [10] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [11] FFUSE     stoi   := 𐑙               𐑙 → 𐑡  | fuse μ — assembly mode
--   [12] TANCH     top    := 𐑡               𐑙 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_imscription_of_the_unit_phases_u_k_5297ca_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_imscription_of_the_unit_phases_u_k_5297ca_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l6 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_imscription_of_the_unit_phases_u_k_5297ca_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_imscription_of_the_unit_phases_u_k_5297ca_protocol : IGProtocol the_imscription_of_the_unit_phases_u_k_5297ca_s0 the_imscription_of_the_unit_phases_u_k_5297ca_s12 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_imscription_of_the_unit_phases_u_k_5297ca_s11 the_imscription_of_the_unit_phases_u_k_5297ca_s11 = the_imscription_of_the_unit_phases_u_k_5297ca_s11 (idempotent)
  (.seq (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l0 the_imscription_of_the_unit_phases_u_k_5297ca_s0 the_imscription_of_the_unit_phases_u_k_5297ca_s1) (.seq (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l1 the_imscription_of_the_unit_phases_u_k_5297ca_s1 the_imscription_of_the_unit_phases_u_k_5297ca_s2) (.seq (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l2 the_imscription_of_the_unit_phases_u_k_5297ca_s2 the_imscription_of_the_unit_phases_u_k_5297ca_s3) (.seq (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l3 the_imscription_of_the_unit_phases_u_k_5297ca_s3 the_imscription_of_the_unit_phases_u_k_5297ca_s4) (.seq (.prod (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l4 the_imscription_of_the_unit_phases_u_k_5297ca_s4 the_imscription_of_the_unit_phases_u_k_5297ca_s11) (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l4 the_imscription_of_the_unit_phases_u_k_5297ca_s4 the_imscription_of_the_unit_phases_u_k_5297ca_s11)) (.seq (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l11 the_imscription_of_the_unit_phases_u_k_5297ca_s11 the_imscription_of_the_unit_phases_u_k_5297ca_s11) (.arrow the_imscription_of_the_unit_phases_u_k_5297ca_l11 the_imscription_of_the_unit_phases_u_k_5297ca_s11 the_imscription_of_the_unit_phases_u_k_5297ca_s12)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_imscription_of_the_unit_phases_u_k_5297ca_true_arm : IGProtocol the_imscription_of_the_unit_phases_u_k_5297ca_s0 the_imscription_of_the_unit_phases_u_k_5297ca_s12 :=
  (the_imscription_of_the_unit_phases_u_k_5297ca_protocol).restrictToEVALT

-- false arm
noncomputable def the_imscription_of_the_unit_phases_u_k_5297ca_false_arm : IGProtocol the_imscription_of_the_unit_phases_u_k_5297ca_s0 the_imscription_of_the_unit_phases_u_k_5297ca_s12 :=
  (the_imscription_of_the_unit_phases_u_k_5297ca_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_imscription_of_the_unit_phases_u_k_5297ca_tier : OuroboricityTier := TierFunctor.obj the_imscription_of_the_unit_phases_u_k_5297ca_s0
#eval the_imscription_of_the_unit_phases_u_k_5297ca_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_imscription_of_the_unit_phases_u_k_5297ca_frobenius :
    igFrobeniusAlg.mul the_imscription_of_the_unit_phases_u_k_5297ca_s0 the_imscription_of_the_unit_phases_u_k_5297ca_s0 = the_imscription_of_the_unit_phases_u_k_5297ca_s0 :=
  igFrobAlg_self_fusion the_imscription_of_the_unit_phases_u_k_5297ca_s0
