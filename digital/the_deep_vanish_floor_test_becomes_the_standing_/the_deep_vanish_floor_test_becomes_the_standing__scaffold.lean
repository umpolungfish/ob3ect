-- IGProtocol scaffold: VINIT → IMSCRIB → CLINK → FSPLIT → AREV → EVALF → FFUSE → CLINK → FSPLIT → AFWD → EVALT → FFUSE → IFIX → TANCH
-- Class: the deep-vanish floor test becomes the standing admission gate for the whole section, admitting a candidate degree or exact coordinate only when its integer polynomial jumps to full precision and plateaus rather than creeping, because the artifact degrees 26 and 48 and 68 and 139 each passed the weaker identity-across-precision and dividing-degree checks and were rejected only by the floor
-- Fingerprint: sig=(7,4,2,1)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=False | period=14
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 6), (8, 11)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑱  | identity — self-imscription
--   [2] CLINK     fid    := 𐑱               𐑠 → 𐑚  | composition — regime coherence
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [6] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [7] CLINK     fid    := 𐑱               𐑙 → 𐑚  | composition — regime coherence
--   [8] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [9] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [10] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [11] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [12] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [13] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_deep_vanish_floor_test_becomes_the_c70d23_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s7 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s8 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_deep_vanish_floor_test_becomes_the_c70d23_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_deep_vanish_floor_test_becomes_the_c70d23_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l9 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_deep_vanish_floor_test_becomes_the_c70d23_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_deep_vanish_floor_test_becomes_the_c70d23_protocol : IGProtocol the_deep_vanish_floor_test_becomes_the_c70d23_s0 the_deep_vanish_floor_test_becomes_the_c70d23_s13 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_deep_vanish_floor_test_becomes_the_c70d23_s6 the_deep_vanish_floor_test_becomes_the_c70d23_s6 = the_deep_vanish_floor_test_becomes_the_c70d23_s6 (idempotent)
  (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l0 the_deep_vanish_floor_test_becomes_the_c70d23_s0 the_deep_vanish_floor_test_becomes_the_c70d23_s1) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l1 the_deep_vanish_floor_test_becomes_the_c70d23_s1 the_deep_vanish_floor_test_becomes_the_c70d23_s2) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l2 the_deep_vanish_floor_test_becomes_the_c70d23_s2 the_deep_vanish_floor_test_becomes_the_c70d23_s3) (.seq (.prod (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l3 the_deep_vanish_floor_test_becomes_the_c70d23_s3 the_deep_vanish_floor_test_becomes_the_c70d23_s6) (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l3 the_deep_vanish_floor_test_becomes_the_c70d23_s3 the_deep_vanish_floor_test_becomes_the_c70d23_s6)) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l6 the_deep_vanish_floor_test_becomes_the_c70d23_s6 the_deep_vanish_floor_test_becomes_the_c70d23_s6) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l6 the_deep_vanish_floor_test_becomes_the_c70d23_s6 the_deep_vanish_floor_test_becomes_the_c70d23_s7) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l7 the_deep_vanish_floor_test_becomes_the_c70d23_s7 the_deep_vanish_floor_test_becomes_the_c70d23_s8) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l8 the_deep_vanish_floor_test_becomes_the_c70d23_s8 the_deep_vanish_floor_test_becomes_the_c70d23_s9) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l9 the_deep_vanish_floor_test_becomes_the_c70d23_s9 the_deep_vanish_floor_test_becomes_the_c70d23_s10) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l10 the_deep_vanish_floor_test_becomes_the_c70d23_s10 the_deep_vanish_floor_test_becomes_the_c70d23_s11) (.seq (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l11 the_deep_vanish_floor_test_becomes_the_c70d23_s11 the_deep_vanish_floor_test_becomes_the_c70d23_s12) (.arrow the_deep_vanish_floor_test_becomes_the_c70d23_l12 the_deep_vanish_floor_test_becomes_the_c70d23_s12 the_deep_vanish_floor_test_becomes_the_c70d23_s13))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_deep_vanish_floor_test_becomes_the_c70d23_true_arm : IGProtocol the_deep_vanish_floor_test_becomes_the_c70d23_s0 the_deep_vanish_floor_test_becomes_the_c70d23_s13 :=
  (the_deep_vanish_floor_test_becomes_the_c70d23_protocol).restrictToEVALT

-- false arm
noncomputable def the_deep_vanish_floor_test_becomes_the_c70d23_false_arm : IGProtocol the_deep_vanish_floor_test_becomes_the_c70d23_s0 the_deep_vanish_floor_test_becomes_the_c70d23_s13 :=
  (the_deep_vanish_floor_test_becomes_the_c70d23_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_deep_vanish_floor_test_becomes_the_c70d23_tier : OuroboricityTier := TierFunctor.obj the_deep_vanish_floor_test_becomes_the_c70d23_s0
#eval the_deep_vanish_floor_test_becomes_the_c70d23_tier  -- the Grammar's own verdict on its tier
