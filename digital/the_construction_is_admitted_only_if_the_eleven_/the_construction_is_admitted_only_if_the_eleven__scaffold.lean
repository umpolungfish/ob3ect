-- IGProtocol scaffold: VINIT → IFIX → IMSCRIB → FSPLIT → CLINK → AFWD → CLINK → EVALT → IFIX → AREV → EVALT → FFUSE → ENGAGR → IFIX → TANCH
-- Class: the construction is admitted only if the eleven a-zero overlaps O sub zero b equal to the sum over k of N_k times omega to the b k for b in one two four eight sixteen thirty two sixty four one hundred twenty eight two hundred fifty six five hundred twelve one thousand twenty four each satisfy O times conjugate equal to one over two thousand forty nine in exact rational arithmetic in the constructed S-unit tower, if every N_k is exhibited as an exact S-unit of F ramified only at the primes three five four hundred nine six hundred eighty three dividing the Appleby discriminant, and if the assembled psi equals B y is genuinely Zauner symmetric in the six hundred eighty three dimensional eigenspace, otherwise the construction is recorded as requiring the ray class field of F above the base S-unit group and the finding is the tower layer at which the moduli become expressible
-- Fingerprint: sig=(7,2,3,3)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=False | period=15
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 11)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑭  | initial object — ground of distinction
--   [1] IFIX      prot   := 𐑭               𐑼 → 𐑠  | irreversible fixation — winding number
--   [2] IMSCRIB   gram   := 𐑠               𐑭 → 𐑚  | identity — self-imscription
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [5] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [6] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [7] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [8] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [9] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [10] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [11] FFUSE     stoi   := 𐑙               𐑙 → 𐑳  | fuse μ — assembly mode
--   [12] ENGAGR    stoi   := 𐑳               𐑙 → 𐑭  | engage paradox — B-state, both arms
--   [13] IFIX      prot   := 𐑭               𐑳 → 𐑡  | irreversible fixation — winding number
--   [14] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_construction_is_admitted_only_if_c437d8_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_construction_is_admitted_only_if_c437d8_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l5 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_construction_is_admitted_only_if_c437d8_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_construction_is_admitted_only_if_c437d8_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_construction_is_admitted_only_if_c437d8_protocol : IGProtocol the_construction_is_admitted_only_if_c437d8_s0 the_construction_is_admitted_only_if_c437d8_s14 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_construction_is_admitted_only_if_c437d8_s11 the_construction_is_admitted_only_if_c437d8_s11 = the_construction_is_admitted_only_if_c437d8_s11 (idempotent)
  (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l0 the_construction_is_admitted_only_if_c437d8_s0 the_construction_is_admitted_only_if_c437d8_s1) (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l1 the_construction_is_admitted_only_if_c437d8_s1 the_construction_is_admitted_only_if_c437d8_s2) (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l2 the_construction_is_admitted_only_if_c437d8_s2 the_construction_is_admitted_only_if_c437d8_s3) (.seq (.prod (.arrow the_construction_is_admitted_only_if_c437d8_l3 the_construction_is_admitted_only_if_c437d8_s3 the_construction_is_admitted_only_if_c437d8_s11) (.arrow the_construction_is_admitted_only_if_c437d8_l3 the_construction_is_admitted_only_if_c437d8_s3 the_construction_is_admitted_only_if_c437d8_s11)) (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l11 the_construction_is_admitted_only_if_c437d8_s11 the_construction_is_admitted_only_if_c437d8_s11) (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l11 the_construction_is_admitted_only_if_c437d8_s11 the_construction_is_admitted_only_if_c437d8_s12) (.seq (.arrow the_construction_is_admitted_only_if_c437d8_l12 the_construction_is_admitted_only_if_c437d8_s12 the_construction_is_admitted_only_if_c437d8_s13) (.arrow the_construction_is_admitted_only_if_c437d8_l13 the_construction_is_admitted_only_if_c437d8_s13 the_construction_is_admitted_only_if_c437d8_s14))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_construction_is_admitted_only_if_c437d8_true_arm : IGProtocol the_construction_is_admitted_only_if_c437d8_s0 the_construction_is_admitted_only_if_c437d8_s14 :=
  (the_construction_is_admitted_only_if_c437d8_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_construction_is_admitted_only_if_c437d8_tier : OuroboricityTier := TierFunctor.obj the_construction_is_admitted_only_if_c437d8_s0
#eval the_construction_is_admitted_only_if_c437d8_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_construction_is_admitted_only_if_c437d8_frobenius :
    igFrobeniusAlg.mul the_construction_is_admitted_only_if_c437d8_s0 the_construction_is_admitted_only_if_c437d8_s0 = the_construction_is_admitted_only_if_c437d8_s0 :=
  igFrobAlg_self_fusion the_construction_is_admitted_only_if_c437d8_s0
