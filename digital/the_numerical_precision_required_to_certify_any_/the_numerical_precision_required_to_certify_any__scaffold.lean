-- IGProtocol scaffold: VINIT → CLINK → IMSCRIB → FSPLIT → EVALF → AREV → ENGAGR → AFWD → EVALT → FFUSE → IFIX → TANCH
-- Class: the numerical precision required to certify any exact relation becomes a computed budget equal to the field degree times the coefficient height in bits, turning artifact-versus-genuine into a decidable precision threshold rather than a guess
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑱  | initial object — ground of distinction
--   [1] CLINK     fid    := 𐑱               𐑼 → 𐑠  | composition — regime coherence
--   [2] IMSCRIB   gram   := 𐑠               𐑱 → 𐑚  | identity — self-imscription
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [5] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [6] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [7] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [8] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [9] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [10] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_numerical_precision_required_to_6a9a81_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := up, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := up, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_numerical_precision_required_to_6a9a81_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_numerical_precision_required_to_6a9a81_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l7 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_numerical_precision_required_to_6a9a81_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_numerical_precision_required_to_6a9a81_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_numerical_precision_required_to_6a9a81_protocol : IGProtocol the_numerical_precision_required_to_6a9a81_s0 the_numerical_precision_required_to_6a9a81_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_numerical_precision_required_to_6a9a81_s9 the_numerical_precision_required_to_6a9a81_s9 = the_numerical_precision_required_to_6a9a81_s9 (idempotent)
  (.seq (.arrow the_numerical_precision_required_to_6a9a81_l0 the_numerical_precision_required_to_6a9a81_s0 the_numerical_precision_required_to_6a9a81_s1) (.seq (.arrow the_numerical_precision_required_to_6a9a81_l1 the_numerical_precision_required_to_6a9a81_s1 the_numerical_precision_required_to_6a9a81_s2) (.seq (.arrow the_numerical_precision_required_to_6a9a81_l2 the_numerical_precision_required_to_6a9a81_s2 the_numerical_precision_required_to_6a9a81_s3) (.seq (.prod (.arrow the_numerical_precision_required_to_6a9a81_l3 the_numerical_precision_required_to_6a9a81_s3 the_numerical_precision_required_to_6a9a81_s9) (.arrow the_numerical_precision_required_to_6a9a81_l3 the_numerical_precision_required_to_6a9a81_s3 the_numerical_precision_required_to_6a9a81_s9)) (.seq (.arrow the_numerical_precision_required_to_6a9a81_l9 the_numerical_precision_required_to_6a9a81_s9 the_numerical_precision_required_to_6a9a81_s9) (.seq (.arrow the_numerical_precision_required_to_6a9a81_l9 the_numerical_precision_required_to_6a9a81_s9 the_numerical_precision_required_to_6a9a81_s10) (.arrow the_numerical_precision_required_to_6a9a81_l10 the_numerical_precision_required_to_6a9a81_s10 the_numerical_precision_required_to_6a9a81_s11)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_numerical_precision_required_to_6a9a81_true_arm : IGProtocol the_numerical_precision_required_to_6a9a81_s0 the_numerical_precision_required_to_6a9a81_s11 :=
  (the_numerical_precision_required_to_6a9a81_protocol).restrictToEVALT

-- false arm
noncomputable def the_numerical_precision_required_to_6a9a81_false_arm : IGProtocol the_numerical_precision_required_to_6a9a81_s0 the_numerical_precision_required_to_6a9a81_s11 :=
  (the_numerical_precision_required_to_6a9a81_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_numerical_precision_required_to_6a9a81_tier : OuroboricityTier := TierFunctor.obj the_numerical_precision_required_to_6a9a81_s0
#eval the_numerical_precision_required_to_6a9a81_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_numerical_precision_required_to_6a9a81_frobenius :
    igFrobeniusAlg.mul the_numerical_precision_required_to_6a9a81_s0 the_numerical_precision_required_to_6a9a81_s0 = the_numerical_precision_required_to_6a9a81_s0 :=
  igFrobAlg_self_fusion the_numerical_precision_required_to_6a9a81_s0
