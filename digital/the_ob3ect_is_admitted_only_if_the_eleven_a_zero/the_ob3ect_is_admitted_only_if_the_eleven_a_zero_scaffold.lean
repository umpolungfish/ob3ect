-- IGProtocol scaffold: VINIT → IMSCRIB → IFIX → IFIX → FSPLIT → EVALT → AFWD → CLINK → EVALF → ENGAGR → AREV → FFUSE → IFIX → TANCH
-- Class: the ob3ect is admitted only if the eleven a-zero identities pass in exact arithmetic in the constructed tower not merely to floating creep, if every pinned modulus is exhibited as an S-unit of F with field norm minus one to the degree times leading coefficient equal to plus or minus one, and if the deliverable explicitly records register separation so that the Belnap transport b fiducial omega to the hamming over square root two thousand forty eight skeleton is never conflated with the Stark shadow fiducial from stark two thousand forty eight, and if deep vanish floor is not claimed until overlap squared residuals are below ten to the minus four thousand at one thousand five hundred digit precision on all eleven reps, otherwise the finding is recorded as infeasibility or wrong tower layer
-- Fingerprint: sig=(6,2,3,3)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=14
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(4, 11)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑭  | identity — self-imscription
--   [2] IFIX      prot   := 𐑭               𐑠 → 𐑭  | irreversible fixation — winding number
--   [3] IFIX      prot   := 𐑭               𐑭 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [7] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [8] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [9] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [10] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [11] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [12] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [13] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_ob3ect_is_admitted_only_if_the_9af256_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_ob3ect_is_admitted_only_if_the_9af256_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l6 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_ob3ect_is_admitted_only_if_the_9af256_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_ob3ect_is_admitted_only_if_the_9af256_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_ob3ect_is_admitted_only_if_the_9af256_protocol : IGProtocol the_ob3ect_is_admitted_only_if_the_9af256_s0 the_ob3ect_is_admitted_only_if_the_9af256_s13 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_ob3ect_is_admitted_only_if_the_9af256_s11 the_ob3ect_is_admitted_only_if_the_9af256_s11 = the_ob3ect_is_admitted_only_if_the_9af256_s11 (idempotent)
  (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l0 the_ob3ect_is_admitted_only_if_the_9af256_s0 the_ob3ect_is_admitted_only_if_the_9af256_s1) (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l1 the_ob3ect_is_admitted_only_if_the_9af256_s1 the_ob3ect_is_admitted_only_if_the_9af256_s2) (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l2 the_ob3ect_is_admitted_only_if_the_9af256_s2 the_ob3ect_is_admitted_only_if_the_9af256_s3) (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l3 the_ob3ect_is_admitted_only_if_the_9af256_s3 the_ob3ect_is_admitted_only_if_the_9af256_s4) (.seq (.prod (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l4 the_ob3ect_is_admitted_only_if_the_9af256_s4 the_ob3ect_is_admitted_only_if_the_9af256_s11) (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l4 the_ob3ect_is_admitted_only_if_the_9af256_s4 the_ob3ect_is_admitted_only_if_the_9af256_s11)) (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l11 the_ob3ect_is_admitted_only_if_the_9af256_s11 the_ob3ect_is_admitted_only_if_the_9af256_s11) (.seq (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l11 the_ob3ect_is_admitted_only_if_the_9af256_s11 the_ob3ect_is_admitted_only_if_the_9af256_s12) (.arrow the_ob3ect_is_admitted_only_if_the_9af256_l12 the_ob3ect_is_admitted_only_if_the_9af256_s12 the_ob3ect_is_admitted_only_if_the_9af256_s13))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_ob3ect_is_admitted_only_if_the_9af256_true_arm : IGProtocol the_ob3ect_is_admitted_only_if_the_9af256_s0 the_ob3ect_is_admitted_only_if_the_9af256_s13 :=
  (the_ob3ect_is_admitted_only_if_the_9af256_protocol).restrictToEVALT

-- false arm
noncomputable def the_ob3ect_is_admitted_only_if_the_9af256_false_arm : IGProtocol the_ob3ect_is_admitted_only_if_the_9af256_s0 the_ob3ect_is_admitted_only_if_the_9af256_s13 :=
  (the_ob3ect_is_admitted_only_if_the_9af256_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_ob3ect_is_admitted_only_if_the_9af256_tier : OuroboricityTier := TierFunctor.obj the_ob3ect_is_admitted_only_if_the_9af256_s0
#eval the_ob3ect_is_admitted_only_if_the_9af256_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_ob3ect_is_admitted_only_if_the_9af256_frobenius :
    igFrobeniusAlg.mul the_ob3ect_is_admitted_only_if_the_9af256_s0 the_ob3ect_is_admitted_only_if_the_9af256_s0 = the_ob3ect_is_admitted_only_if_the_9af256_s0 :=
  igFrobAlg_self_fusion the_ob3ect_is_admitted_only_if_the_9af256_s0
