-- IGProtocol scaffold: VINIT → AFWD → IFIX → FSPLIT → EVALT → FSPLIT → EVALF → AREV → IFIX → ENGAGR → FFUSE → CLINK → FFUSE → IMSCRIB → TANCH
-- Class: the polar decomposition of each coordinate into a real magnitude times a unit phase becomes a valid closed-form route rather than a ruled-out one, because the phase is not required to lie inside the degree-288 ray class field but instead lives in the magnitude-extended double cover, so its leaving K288 through the square-root of the modulus S-unit is exactly the field it is closed in and not a disqualification
-- Fingerprint: sig=(6,4,3,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=15
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(5, 10), (3, 12)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑭  | forward morphism — bidirectional arrow
--   [2] IFIX      prot   := 𐑭               𐑾 → 𐑚  | irreversible fixation — winding number
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [6] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [7] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [8] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [9] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [10] FFUSE     stoi   := 𐑙               𐑙 → 𐑙  | fuse μ — assembly mode
--   [11] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [12] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [13] IMSCRIB   gram   := 𐑠               𐑙 → 𐑡  | identity — self-imscription
--   [14] TANCH     top    := 𐑡               𐑠 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_polar_decomposition_of_each_11ce85_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_polar_decomposition_of_each_11ce85_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_polar_decomposition_of_each_11ce85_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_polar_decomposition_of_each_11ce85_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_polar_decomposition_of_each_11ce85_protocol : IGProtocol the_polar_decomposition_of_each_11ce85_s0 the_polar_decomposition_of_each_11ce85_s14 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_polar_decomposition_of_each_11ce85_s10 the_polar_decomposition_of_each_11ce85_s10 = the_polar_decomposition_of_each_11ce85_s10 (idempotent)
  (.seq (.arrow the_polar_decomposition_of_each_11ce85_l0 the_polar_decomposition_of_each_11ce85_s0 the_polar_decomposition_of_each_11ce85_s1) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l1 the_polar_decomposition_of_each_11ce85_s1 the_polar_decomposition_of_each_11ce85_s2) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l2 the_polar_decomposition_of_each_11ce85_s2 the_polar_decomposition_of_each_11ce85_s3) (.seq (.prod (.arrow the_polar_decomposition_of_each_11ce85_l3 the_polar_decomposition_of_each_11ce85_s3 the_polar_decomposition_of_each_11ce85_s10) (.arrow the_polar_decomposition_of_each_11ce85_l3 the_polar_decomposition_of_each_11ce85_s3 the_polar_decomposition_of_each_11ce85_s10)) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l10 the_polar_decomposition_of_each_11ce85_s10 the_polar_decomposition_of_each_11ce85_s10) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l10 the_polar_decomposition_of_each_11ce85_s10 the_polar_decomposition_of_each_11ce85_s11) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l11 the_polar_decomposition_of_each_11ce85_s11 the_polar_decomposition_of_each_11ce85_s12) (.seq (.arrow the_polar_decomposition_of_each_11ce85_l12 the_polar_decomposition_of_each_11ce85_s12 the_polar_decomposition_of_each_11ce85_s13) (.arrow the_polar_decomposition_of_each_11ce85_l13 the_polar_decomposition_of_each_11ce85_s13 the_polar_decomposition_of_each_11ce85_s14)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_polar_decomposition_of_each_11ce85_true_arm : IGProtocol the_polar_decomposition_of_each_11ce85_s0 the_polar_decomposition_of_each_11ce85_s14 :=
  (the_polar_decomposition_of_each_11ce85_protocol).restrictToEVALT

-- false arm
noncomputable def the_polar_decomposition_of_each_11ce85_false_arm : IGProtocol the_polar_decomposition_of_each_11ce85_s0 the_polar_decomposition_of_each_11ce85_s14 :=
  (the_polar_decomposition_of_each_11ce85_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_polar_decomposition_of_each_11ce85_tier : OuroboricityTier := TierFunctor.obj the_polar_decomposition_of_each_11ce85_s0
#eval the_polar_decomposition_of_each_11ce85_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_polar_decomposition_of_each_11ce85_frobenius :
    igFrobeniusAlg.mul the_polar_decomposition_of_each_11ce85_s0 the_polar_decomposition_of_each_11ce85_s0 = the_polar_decomposition_of_each_11ce85_s0 :=
  igFrobAlg_self_fusion the_polar_decomposition_of_each_11ce85_s0
