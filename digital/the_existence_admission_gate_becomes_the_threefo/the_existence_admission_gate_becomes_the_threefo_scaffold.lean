-- IGProtocol scaffold: VINIT → AFWD → IMSCRIB → FSPLIT → EVALF → AREV → EVALT → FFUSE → CLINK → ENGAGR → IFIX → TANCH
-- Class: the existence admission gate becomes the threefold requirement that the constructed coordinates reproduce the 1500-digit witness to the deep-vanish floor, that every in-tower identity is decided true under native_decide in the flat engine, and that each constructed overlap's minimal polynomial equals its banked pinned polynomial exactly, rejecting any tower layer whose reduction merely creeps with precision as the signature of a coordinate placed in the wrong layer
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(3, 7)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑠  | forward morphism — bidirectional arrow
--   [2] IMSCRIB   gram   := 𐑠               𐑾 → 𐑚  | identity — self-imscription
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [5] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [6] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [7] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [8] CLINK     fid    := 𐑱               𐑙 → 𐑳  | composition — regime coherence
--   [9] ENGAGR    stoi   := 𐑳               𐑱 → 𐑭  | engage paradox — B-state, both arms
--   [10] IFIX      prot   := 𐑭               𐑳 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_existence_admission_gate_becomes_b62a64_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_existence_admission_gate_becomes_b62a64_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_existence_admission_gate_becomes_b62a64_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_existence_admission_gate_becomes_b62a64_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_existence_admission_gate_becomes_b62a64_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_existence_admission_gate_becomes_b62a64_protocol : IGProtocol the_existence_admission_gate_becomes_b62a64_s0 the_existence_admission_gate_becomes_b62a64_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_existence_admission_gate_becomes_b62a64_s7 the_existence_admission_gate_becomes_b62a64_s7 = the_existence_admission_gate_becomes_b62a64_s7 (idempotent)
  (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l0 the_existence_admission_gate_becomes_b62a64_s0 the_existence_admission_gate_becomes_b62a64_s1) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l1 the_existence_admission_gate_becomes_b62a64_s1 the_existence_admission_gate_becomes_b62a64_s2) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l2 the_existence_admission_gate_becomes_b62a64_s2 the_existence_admission_gate_becomes_b62a64_s3) (.seq (.prod (.arrow the_existence_admission_gate_becomes_b62a64_l3 the_existence_admission_gate_becomes_b62a64_s3 the_existence_admission_gate_becomes_b62a64_s7) (.arrow the_existence_admission_gate_becomes_b62a64_l3 the_existence_admission_gate_becomes_b62a64_s3 the_existence_admission_gate_becomes_b62a64_s7)) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l7 the_existence_admission_gate_becomes_b62a64_s7 the_existence_admission_gate_becomes_b62a64_s7) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l7 the_existence_admission_gate_becomes_b62a64_s7 the_existence_admission_gate_becomes_b62a64_s8) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l8 the_existence_admission_gate_becomes_b62a64_s8 the_existence_admission_gate_becomes_b62a64_s9) (.seq (.arrow the_existence_admission_gate_becomes_b62a64_l9 the_existence_admission_gate_becomes_b62a64_s9 the_existence_admission_gate_becomes_b62a64_s10) (.arrow the_existence_admission_gate_becomes_b62a64_l10 the_existence_admission_gate_becomes_b62a64_s10 the_existence_admission_gate_becomes_b62a64_s11)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_existence_admission_gate_becomes_b62a64_true_arm : IGProtocol the_existence_admission_gate_becomes_b62a64_s0 the_existence_admission_gate_becomes_b62a64_s11 :=
  (the_existence_admission_gate_becomes_b62a64_protocol).restrictToEVALT

-- false arm
noncomputable def the_existence_admission_gate_becomes_b62a64_false_arm : IGProtocol the_existence_admission_gate_becomes_b62a64_s0 the_existence_admission_gate_becomes_b62a64_s11 :=
  (the_existence_admission_gate_becomes_b62a64_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_existence_admission_gate_becomes_b62a64_tier : OuroboricityTier := TierFunctor.obj the_existence_admission_gate_becomes_b62a64_s0
#eval the_existence_admission_gate_becomes_b62a64_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_existence_admission_gate_becomes_b62a64_frobenius :
    igFrobeniusAlg.mul the_existence_admission_gate_becomes_b62a64_s0 the_existence_admission_gate_becomes_b62a64_s0 = the_existence_admission_gate_becomes_b62a64_s0 :=
  igFrobAlg_self_fusion the_existence_admission_gate_becomes_b62a64_s0
