-- IGProtocol scaffold: VINIT → IMSCRIB → AFWD → IFIX → FSPLIT → EVALT → CLINK → AREV → CLINK → IMSCRIB → AFWD → CLINK → FSPLIT → EVALT → FFUSE → EVALF → ENGAGR → FFUSE → IFIX → CLINK → TANCH
-- Class: the closure is admitted only under the threefold gate that the constructed coordinates reproduce the numerical witness to the deep-vanish floor, that every in-ring identity over the orbit representatives is decided true by native_decide in the flat engine, and that each constructed overlap's minimal polynomial equals its independently pinned polynomial exactly, with any tower layer whose reduction merely creeps with precision rejected as the signature of a coordinate placed in the wrong field, and with the whole campaign dialetheia-honest that at d equals two thousand forty eight the true outcome may be a floor-certified numerical fiducial with the exact in-Lean drop left as a named remaining plank rather than a completed theorem, either outcome recorded as a finding under the founding principle that there are no walls only admission gates and the deep-vanish floor is that gate
-- Fingerprint: sig=(11,4,4,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=21
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(12, 14), (4, 17)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑾  | identity — self-imscription
--   [2] AFWD      rel    := 𐑾               𐑠 → 𐑭  | forward morphism — bidirectional arrow
--   [3] IFIX      prot   := 𐑭               𐑾 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [7] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [8] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [9] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [10] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [11] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [12] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [13] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [14] FFUSE     stoi   := 𐑙               𐑙 → 𐑙  | fuse μ — assembly mode
--   [15] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [16] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [17] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [18] IFIX      prot   := 𐑭               𐑙 → 𐑱  | irreversible fixation — winding number
--   [19] CLINK     fid    := 𐑱               𐑭 → 𐑡  | composition — regime coherence
--   [20] TANCH     top    := 𐑡               𐑱 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_closure_is_admitted_only_under_the_a6d0aa_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s17 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s18 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s19 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_s20 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_closure_is_admitted_only_under_the_a6d0aa_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l2 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l10 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l17 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l18 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_closure_is_admitted_only_under_the_a6d0aa_l19 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_closure_is_admitted_only_under_the_a6d0aa_l20 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_closure_is_admitted_only_under_the_a6d0aa_protocol : IGProtocol the_closure_is_admitted_only_under_the_a6d0aa_s0 the_closure_is_admitted_only_under_the_a6d0aa_s20 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_closure_is_admitted_only_under_the_a6d0aa_s14 the_closure_is_admitted_only_under_the_a6d0aa_s14 = the_closure_is_admitted_only_under_the_a6d0aa_s14 (idempotent)
  (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l0 the_closure_is_admitted_only_under_the_a6d0aa_s0 the_closure_is_admitted_only_under_the_a6d0aa_s1) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l1 the_closure_is_admitted_only_under_the_a6d0aa_s1 the_closure_is_admitted_only_under_the_a6d0aa_s2) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l2 the_closure_is_admitted_only_under_the_a6d0aa_s2 the_closure_is_admitted_only_under_the_a6d0aa_s3) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l3 the_closure_is_admitted_only_under_the_a6d0aa_s3 the_closure_is_admitted_only_under_the_a6d0aa_s4) (.seq (.prod (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l4 the_closure_is_admitted_only_under_the_a6d0aa_s4 the_closure_is_admitted_only_under_the_a6d0aa_s14) (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l4 the_closure_is_admitted_only_under_the_a6d0aa_s4 the_closure_is_admitted_only_under_the_a6d0aa_s14)) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l14 the_closure_is_admitted_only_under_the_a6d0aa_s14 the_closure_is_admitted_only_under_the_a6d0aa_s14) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l14 the_closure_is_admitted_only_under_the_a6d0aa_s14 the_closure_is_admitted_only_under_the_a6d0aa_s15) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l15 the_closure_is_admitted_only_under_the_a6d0aa_s15 the_closure_is_admitted_only_under_the_a6d0aa_s16) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l16 the_closure_is_admitted_only_under_the_a6d0aa_s16 the_closure_is_admitted_only_under_the_a6d0aa_s17) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l17 the_closure_is_admitted_only_under_the_a6d0aa_s17 the_closure_is_admitted_only_under_the_a6d0aa_s18) (.seq (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l18 the_closure_is_admitted_only_under_the_a6d0aa_s18 the_closure_is_admitted_only_under_the_a6d0aa_s19) (.arrow the_closure_is_admitted_only_under_the_a6d0aa_l19 the_closure_is_admitted_only_under_the_a6d0aa_s19 the_closure_is_admitted_only_under_the_a6d0aa_s20))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_closure_is_admitted_only_under_the_a6d0aa_true_arm : IGProtocol the_closure_is_admitted_only_under_the_a6d0aa_s0 the_closure_is_admitted_only_under_the_a6d0aa_s20 :=
  (the_closure_is_admitted_only_under_the_a6d0aa_protocol).restrictToEVALT

-- false arm
noncomputable def the_closure_is_admitted_only_under_the_a6d0aa_false_arm : IGProtocol the_closure_is_admitted_only_under_the_a6d0aa_s0 the_closure_is_admitted_only_under_the_a6d0aa_s20 :=
  (the_closure_is_admitted_only_under_the_a6d0aa_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_closure_is_admitted_only_under_the_a6d0aa_tier : OuroboricityTier := TierFunctor.obj the_closure_is_admitted_only_under_the_a6d0aa_s0
#eval the_closure_is_admitted_only_under_the_a6d0aa_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_closure_is_admitted_only_under_the_a6d0aa_frobenius :
    igFrobeniusAlg.mul the_closure_is_admitted_only_under_the_a6d0aa_s0 the_closure_is_admitted_only_under_the_a6d0aa_s0 = the_closure_is_admitted_only_under_the_a6d0aa_s0 :=
  igFrobAlg_self_fusion the_closure_is_admitted_only_under_the_a6d0aa_s0
