-- IGProtocol scaffold: VINIT → IMSCRIB → IFIX → FSPLIT → EVALT → AFWD → CLINK → AFWD → IFIX → AFWD → EVALF → ENGAGR → AREV → FFUSE → FSPLIT → EVALT → IMSCRIB → FFUSE → IFIX → TANCH
-- Class: each tower level is a solve et coagula distillation adjoining one square root determined by the S-unit and class field data at that level, the fourth root of unity structure of the pro-two tower two to the twelve times two to the nine times two to the three times two to the two times two to the one meaning the moduli are reached not by a single degree two to the twenty seven minimal polynomial but by twenty seven successive quadratic ascents, where the unramified levels up to the Hilbert class field of degree sixty four over F carry the class group thirty two by two and the ramified levels above them at conductor two thousand forty eight times both infinite places carry the remaining two to the twenty one, and the flat autocorrelation condition C zero equals two over two thousand forty nine and C m equals one over two thousand forty nine pins the moduli as the tower closes
-- Fingerprint: sig=(9,4,4,3)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=True | period=20
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 13), (14, 17)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑭  | identity — self-imscription
--   [2] IFIX      prot   := 𐑭               𐑠 → 𐑚  | irreversible fixation — winding number
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [6] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [7] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [8] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [9] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [10] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [11] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [12] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [13] FFUSE     stoi   := 𐑙               𐑙 → 𐑙  | fuse μ — assembly mode
--   [14] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [15] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [16] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [17] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [18] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [19] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s17 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s18 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_s19 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l5 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l7 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l9 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l17 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l18 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def each_tower_level_is_a_solve_et_coagula_8ca0ac_l19 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def each_tower_level_is_a_solve_et_coagula_8ca0ac_protocol : IGProtocol each_tower_level_is_a_solve_et_coagula_8ca0ac_s0 each_tower_level_is_a_solve_et_coagula_8ca0ac_s19 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 = each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 (idempotent)
  (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l0 each_tower_level_is_a_solve_et_coagula_8ca0ac_s0 each_tower_level_is_a_solve_et_coagula_8ca0ac_s1) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l1 each_tower_level_is_a_solve_et_coagula_8ca0ac_s1 each_tower_level_is_a_solve_et_coagula_8ca0ac_s2) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l2 each_tower_level_is_a_solve_et_coagula_8ca0ac_s2 each_tower_level_is_a_solve_et_coagula_8ca0ac_s3) (.seq (.prod (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l3 each_tower_level_is_a_solve_et_coagula_8ca0ac_s3 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13) (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l3 each_tower_level_is_a_solve_et_coagula_8ca0ac_s3 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13)) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l13 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l13 each_tower_level_is_a_solve_et_coagula_8ca0ac_s13 each_tower_level_is_a_solve_et_coagula_8ca0ac_s14) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l14 each_tower_level_is_a_solve_et_coagula_8ca0ac_s14 each_tower_level_is_a_solve_et_coagula_8ca0ac_s15) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l15 each_tower_level_is_a_solve_et_coagula_8ca0ac_s15 each_tower_level_is_a_solve_et_coagula_8ca0ac_s16) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l16 each_tower_level_is_a_solve_et_coagula_8ca0ac_s16 each_tower_level_is_a_solve_et_coagula_8ca0ac_s17) (.seq (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l17 each_tower_level_is_a_solve_et_coagula_8ca0ac_s17 each_tower_level_is_a_solve_et_coagula_8ca0ac_s18) (.arrow each_tower_level_is_a_solve_et_coagula_8ca0ac_l18 each_tower_level_is_a_solve_et_coagula_8ca0ac_s18 each_tower_level_is_a_solve_et_coagula_8ca0ac_s19)))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def each_tower_level_is_a_solve_et_coagula_8ca0ac_true_arm : IGProtocol each_tower_level_is_a_solve_et_coagula_8ca0ac_s0 each_tower_level_is_a_solve_et_coagula_8ca0ac_s19 :=
  (each_tower_level_is_a_solve_et_coagula_8ca0ac_protocol).restrictToEVALT

-- false arm
noncomputable def each_tower_level_is_a_solve_et_coagula_8ca0ac_false_arm : IGProtocol each_tower_level_is_a_solve_et_coagula_8ca0ac_s0 each_tower_level_is_a_solve_et_coagula_8ca0ac_s19 :=
  (each_tower_level_is_a_solve_et_coagula_8ca0ac_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def each_tower_level_is_a_solve_et_coagula_8ca0ac_tier : OuroboricityTier := TierFunctor.obj each_tower_level_is_a_solve_et_coagula_8ca0ac_s0
#eval each_tower_level_is_a_solve_et_coagula_8ca0ac_tier  -- the Grammar's own verdict on its tier
