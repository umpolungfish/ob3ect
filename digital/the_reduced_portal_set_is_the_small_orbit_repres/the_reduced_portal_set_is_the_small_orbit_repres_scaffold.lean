-- IGProtocol scaffold: VINIT → AFWD → IFIX → IMSCRIB → FSPLIT → EVALT → CLINK → EVALF → ENGAGR → CLINK → AREV → FFUSE → IMSCRIB → IFIX → TANCH
-- Class: the reduced portal set is the small orbit representative family of ray class characters that see the a-zero stratum, obtained because a brute force L-derivative over all two to the twenty five characters is infeasible but the a-zero moduli are only one thousand twenty four real numbers halved from two thousand forty eight by the Galois pairing N sub k plus one thousand twenty four equals sigma of N sub k and pinned by the flat autocorrelation C zero equals two over two thousand forty nine and C m equals one over two thousand forty nine which is the discrete Fourier Wiener Khinchin condition, so the Galois action on the character group of the totally real conductor two thousand forty eight ray class field together with the complex conjugation mirror pairing collapses the millions of portals to the representatives whose s equals zero L-derivatives determine all the moduli
-- Fingerprint: sig=(8,2,3,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=15
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(4, 11)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑭  | forward morphism — bidirectional arrow
--   [2] IFIX      prot   := 𐑭               𐑾 → 𐑠  | irreversible fixation — winding number
--   [3] IMSCRIB   gram   := 𐑠               𐑭 → 𐑚  | identity — self-imscription
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [7] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [10] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [11] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [12] IMSCRIB   gram   := 𐑠               𐑙 → 𐑭  | identity — self-imscription
--   [13] IFIX      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [14] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_reduced_portal_set_is_the_small_1a7564_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_reduced_portal_set_is_the_small_1a7564_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_reduced_portal_set_is_the_small_1a7564_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_reduced_portal_set_is_the_small_1a7564_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_reduced_portal_set_is_the_small_1a7564_protocol : IGProtocol the_reduced_portal_set_is_the_small_1a7564_s0 the_reduced_portal_set_is_the_small_1a7564_s14 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_reduced_portal_set_is_the_small_1a7564_s11 the_reduced_portal_set_is_the_small_1a7564_s11 = the_reduced_portal_set_is_the_small_1a7564_s11 (idempotent)
  (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l0 the_reduced_portal_set_is_the_small_1a7564_s0 the_reduced_portal_set_is_the_small_1a7564_s1) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l1 the_reduced_portal_set_is_the_small_1a7564_s1 the_reduced_portal_set_is_the_small_1a7564_s2) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l2 the_reduced_portal_set_is_the_small_1a7564_s2 the_reduced_portal_set_is_the_small_1a7564_s3) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l3 the_reduced_portal_set_is_the_small_1a7564_s3 the_reduced_portal_set_is_the_small_1a7564_s4) (.seq (.prod (.arrow the_reduced_portal_set_is_the_small_1a7564_l4 the_reduced_portal_set_is_the_small_1a7564_s4 the_reduced_portal_set_is_the_small_1a7564_s11) (.arrow the_reduced_portal_set_is_the_small_1a7564_l4 the_reduced_portal_set_is_the_small_1a7564_s4 the_reduced_portal_set_is_the_small_1a7564_s11)) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l11 the_reduced_portal_set_is_the_small_1a7564_s11 the_reduced_portal_set_is_the_small_1a7564_s11) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l11 the_reduced_portal_set_is_the_small_1a7564_s11 the_reduced_portal_set_is_the_small_1a7564_s12) (.seq (.arrow the_reduced_portal_set_is_the_small_1a7564_l12 the_reduced_portal_set_is_the_small_1a7564_s12 the_reduced_portal_set_is_the_small_1a7564_s13) (.arrow the_reduced_portal_set_is_the_small_1a7564_l13 the_reduced_portal_set_is_the_small_1a7564_s13 the_reduced_portal_set_is_the_small_1a7564_s14)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_reduced_portal_set_is_the_small_1a7564_true_arm : IGProtocol the_reduced_portal_set_is_the_small_1a7564_s0 the_reduced_portal_set_is_the_small_1a7564_s14 :=
  (the_reduced_portal_set_is_the_small_1a7564_protocol).restrictToEVALT

-- false arm
noncomputable def the_reduced_portal_set_is_the_small_1a7564_false_arm : IGProtocol the_reduced_portal_set_is_the_small_1a7564_s0 the_reduced_portal_set_is_the_small_1a7564_s14 :=
  (the_reduced_portal_set_is_the_small_1a7564_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_reduced_portal_set_is_the_small_1a7564_tier : OuroboricityTier := TierFunctor.obj the_reduced_portal_set_is_the_small_1a7564_s0
#eval the_reduced_portal_set_is_the_small_1a7564_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_reduced_portal_set_is_the_small_1a7564_frobenius :
    igFrobeniusAlg.mul the_reduced_portal_set_is_the_small_1a7564_s0 the_reduced_portal_set_is_the_small_1a7564_s0 = the_reduced_portal_set_is_the_small_1a7564_s0 :=
  igFrobAlg_self_fusion the_reduced_portal_set_is_the_small_1a7564_s0
