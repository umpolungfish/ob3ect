-- IGProtocol scaffold: VINIT → IMSCRIB → CLINK → FSPLIT → AFWD → EVALT → AREV → EVALF → ENGAGR → FFUSE → IFIX → TANCH
-- Class: structural imscription of the mechanism a CLI expansion degree must be: a dial that carries a kernel-verified proof (the T/F-lane Witness) between its pinched one-line form and its walked-out multi-line form, the same kernel truth held in both as a B state, forcing the choice between an LLM targeting a line count and a deterministic elaboration ladder. With full Frobenius closure and Lean 4 verification scaffold.
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑱  | identity — self-imscription
--   [2] CLINK     fid    := 𐑱               𐑠 → 𐑚  | composition — regime coherence
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [10] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def structural_imscription_of_the_mechanism_b2944b_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def structural_imscription_of_the_mechanism_b2944b_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def structural_imscription_of_the_mechanism_b2944b_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l4 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def structural_imscription_of_the_mechanism_b2944b_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def structural_imscription_of_the_mechanism_b2944b_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def structural_imscription_of_the_mechanism_b2944b_protocol : IGProtocol structural_imscription_of_the_mechanism_b2944b_s0 structural_imscription_of_the_mechanism_b2944b_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct structural_imscription_of_the_mechanism_b2944b_s9 structural_imscription_of_the_mechanism_b2944b_s9 = structural_imscription_of_the_mechanism_b2944b_s9 (idempotent)
  (.seq (.arrow structural_imscription_of_the_mechanism_b2944b_l0 structural_imscription_of_the_mechanism_b2944b_s0 structural_imscription_of_the_mechanism_b2944b_s1) (.seq (.arrow structural_imscription_of_the_mechanism_b2944b_l1 structural_imscription_of_the_mechanism_b2944b_s1 structural_imscription_of_the_mechanism_b2944b_s2) (.seq (.arrow structural_imscription_of_the_mechanism_b2944b_l2 structural_imscription_of_the_mechanism_b2944b_s2 structural_imscription_of_the_mechanism_b2944b_s3) (.seq (.prod (.arrow structural_imscription_of_the_mechanism_b2944b_l3 structural_imscription_of_the_mechanism_b2944b_s3 structural_imscription_of_the_mechanism_b2944b_s9) (.arrow structural_imscription_of_the_mechanism_b2944b_l3 structural_imscription_of_the_mechanism_b2944b_s3 structural_imscription_of_the_mechanism_b2944b_s9)) (.seq (.arrow structural_imscription_of_the_mechanism_b2944b_l9 structural_imscription_of_the_mechanism_b2944b_s9 structural_imscription_of_the_mechanism_b2944b_s9) (.seq (.arrow structural_imscription_of_the_mechanism_b2944b_l9 structural_imscription_of_the_mechanism_b2944b_s9 structural_imscription_of_the_mechanism_b2944b_s10) (.arrow structural_imscription_of_the_mechanism_b2944b_l10 structural_imscription_of_the_mechanism_b2944b_s10 structural_imscription_of_the_mechanism_b2944b_s11)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def structural_imscription_of_the_mechanism_b2944b_true_arm : IGProtocol structural_imscription_of_the_mechanism_b2944b_s0 structural_imscription_of_the_mechanism_b2944b_s11 :=
  (structural_imscription_of_the_mechanism_b2944b_protocol).restrictToEVALT

-- false arm
noncomputable def structural_imscription_of_the_mechanism_b2944b_false_arm : IGProtocol structural_imscription_of_the_mechanism_b2944b_s0 structural_imscription_of_the_mechanism_b2944b_s11 :=
  (structural_imscription_of_the_mechanism_b2944b_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def structural_imscription_of_the_mechanism_b2944b_tier : OuroboricityTier := TierFunctor.obj structural_imscription_of_the_mechanism_b2944b_s0
#eval structural_imscription_of_the_mechanism_b2944b_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem structural_imscription_of_the_mechanism_b2944b_frobenius :
    igFrobeniusAlg.mul structural_imscription_of_the_mechanism_b2944b_s0 structural_imscription_of_the_mechanism_b2944b_s0 = structural_imscription_of_the_mechanism_b2944b_s0 :=
  igFrobAlg_self_fusion structural_imscription_of_the_mechanism_b2944b_s0
