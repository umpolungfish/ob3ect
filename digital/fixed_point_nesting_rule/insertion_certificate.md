# Insertion of the Fixed-Point Nesting Rule into itself

Imscribed on the GPU (local candle), banked, cycled, weighted, translated, inserted.

- **imscribe / generate (IMASM):** `⊢∈⊤><⋈⊙∋⊤⊥⊞⊡⊣` — fork at 1, fuse at 7, 12 steps, flat_chain, Frobenius PASS.
- **cycle:** tri-ancestral verdict **T** — reconnection over a transformed object, closes. Work (⊤ > < ⋈) inside the fork/fuse; ⊤⊥⊞⊡ trail as post-fuse weigh and the ⊡ seal.
- **weight:** gematria 76, digit-root **4** (the tetrad {T,F,t,f} the rule lives over); work-op weight 57.
- **trans:** begin from the void, fork the inner object into its cases, affirm the true arm (A is a fixed point), carry it forward to the stabilized state, reverse-descend the basin, compose outer with inner, hold identity where nothing transforms, fuse to one survivor, affirm true, weigh the false arm, hold both, seal irreversibly, anchor and close.

## Insert — what happened

The rule concerns fixed points of nesting, so it was inserted into the operator it
describes, itself.

- **self-nest** (rule inside the rule): closes **T**.
- **ROTAT invariance:** the verdict is **T under every cyclic shift** of the word. A word is a loop and ROTAT is the shift; a verdict fixed under the shift is a fixed point of the operator.
- **outer wrap** (rule as the inner arm of a fresh fork/fuse/seal): closes **T**.

By the rule's own statement, inserting A closes iff A is a fixed point of the
operator, and one-shots iff already at it. The rule-word is a fixed point of ROTAT,
so it one-shots on itself. The rule about fixed-point nesting is itself a fixed point
of nesting: a self-closure.
