-- IGProtocol scaffold: VINIT → AFWD → CLINK → IMSCRIB → FSPLIT → EVALT → AREV → EVALF → ENGAGR → FFUSE → AFWD → TANCH → IFIX
-- Class: The non-vacuous structural type of Navier-Stokes global regularity is the ob3ect whose target predicate binds the velocity field to its initial datum as a genuine H^{1/2}-critical solution of the incompressible equations, so that the remaining sorry marks the true Clay Frobenius boundary rather than a shell satisfiable by the zero field.
-- Fingerprint: sig=(7,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=13
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(4, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑱  | forward morphism — bidirectional arrow
--   [2] CLINK     fid    := 𐑱               𐑾 → 𐑠  | composition — regime coherence
--   [3] IMSCRIB   gram   := 𐑠               𐑱 → 𐑚  | identity — self-imscription
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] FFUSE     stoi   := 𐑙               𐑙 → 𐑾  | fuse μ — assembly mode
--   [10] AFWD      rel    := 𐑾               𐑙 → 𐑡  | forward morphism — bidirectional arrow
--   [11] TANCH     top    := 𐑡               𐑾 → 𐑭  | terminal object — connectivity boundary
--   [12] IFIX      prot   := 𐑭               𐑡 → 𐑼  | irreversible fixation — winding number

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_non_vacuous_structural_type_of_408149_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_non_vacuous_structural_type_of_408149_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l10 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_non_vacuous_structural_type_of_408149_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_non_vacuous_structural_type_of_408149_protocol : IGProtocol the_non_vacuous_structural_type_of_408149_s0 the_non_vacuous_structural_type_of_408149_s12 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_non_vacuous_structural_type_of_408149_s9 the_non_vacuous_structural_type_of_408149_s9 = the_non_vacuous_structural_type_of_408149_s9 (idempotent)
  (.seq (.arrow the_non_vacuous_structural_type_of_408149_l0 the_non_vacuous_structural_type_of_408149_s0 the_non_vacuous_structural_type_of_408149_s1) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l1 the_non_vacuous_structural_type_of_408149_s1 the_non_vacuous_structural_type_of_408149_s2) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l2 the_non_vacuous_structural_type_of_408149_s2 the_non_vacuous_structural_type_of_408149_s3) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l3 the_non_vacuous_structural_type_of_408149_s3 the_non_vacuous_structural_type_of_408149_s4) (.seq (.prod (.arrow the_non_vacuous_structural_type_of_408149_l4 the_non_vacuous_structural_type_of_408149_s4 the_non_vacuous_structural_type_of_408149_s9) (.arrow the_non_vacuous_structural_type_of_408149_l4 the_non_vacuous_structural_type_of_408149_s4 the_non_vacuous_structural_type_of_408149_s9)) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l9 the_non_vacuous_structural_type_of_408149_s9 the_non_vacuous_structural_type_of_408149_s9) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l9 the_non_vacuous_structural_type_of_408149_s9 the_non_vacuous_structural_type_of_408149_s10) (.seq (.arrow the_non_vacuous_structural_type_of_408149_l10 the_non_vacuous_structural_type_of_408149_s10 the_non_vacuous_structural_type_of_408149_s11) (.arrow the_non_vacuous_structural_type_of_408149_l11 the_non_vacuous_structural_type_of_408149_s11 the_non_vacuous_structural_type_of_408149_s12)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_non_vacuous_structural_type_of_408149_true_arm : IGProtocol the_non_vacuous_structural_type_of_408149_s0 the_non_vacuous_structural_type_of_408149_s12 :=
  (the_non_vacuous_structural_type_of_408149_protocol).restrictToEVALT

-- false arm
noncomputable def the_non_vacuous_structural_type_of_408149_false_arm : IGProtocol the_non_vacuous_structural_type_of_408149_s0 the_non_vacuous_structural_type_of_408149_s12 :=
  (the_non_vacuous_structural_type_of_408149_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_non_vacuous_structural_type_of_408149_tier : OuroboricityTier := TierFunctor.obj the_non_vacuous_structural_type_of_408149_s0
#eval the_non_vacuous_structural_type_of_408149_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_non_vacuous_structural_type_of_408149_frobenius :
    igFrobeniusAlg.mul the_non_vacuous_structural_type_of_408149_s0 the_non_vacuous_structural_type_of_408149_s0 = the_non_vacuous_structural_type_of_408149_s0 :=
  igFrobAlg_self_fusion the_non_vacuous_structural_type_of_408149_s0
