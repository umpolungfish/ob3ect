-- IGProtocol scaffold: VINIT → FSPLIT → AFWD → EVALT → AREV → EVALF → FFUSE → IMSCRIB → CLINK → ENGAGR → CLINK → IMSCRIB → IFIX → TANCH
-- Class: the P-versus-NP tuple that fails at the gate layer under all twenty-nine existing universes becomes a new independently-motivated gate-universe whose gate specification separates the verification arm from the search arm and carries it to idempotent_terminal while staying T_CEILING-consistent
-- Fingerprint: sig=(8,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=14
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(1, 6)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑚  | initial object — ground of distinction
--   [1] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [2] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [3] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [4] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [6] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [7] IMSCRIB   gram   := 𐑠               𐑙 → 𐑱  | identity — self-imscription
--   [8] CLINK     fid    := 𐑱               𐑠 → 𐑳  | composition — regime coherence
--   [9] ENGAGR    stoi   := 𐑳               𐑱 → 𐑱  | engage paradox — B-state, both arms
--   [10] CLINK     fid    := 𐑱               𐑳 → 𐑠  | composition — regime coherence
--   [11] IMSCRIB   gram   := 𐑠               𐑱 → 𐑭  | identity — self-imscription
--   [12] IFIX      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [13] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l2 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_p_versus_np_tuple_that_fails_at_the_5c3959_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_p_versus_np_tuple_that_fails_at_the_5c3959_protocol : IGProtocol the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s13 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 = the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 (idempotent)
  (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s1) (.seq (.prod (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l1 the_p_versus_np_tuple_that_fails_at_the_5c3959_s1 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6) (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l1 the_p_versus_np_tuple_that_fails_at_the_5c3959_s1 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6)) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l6 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l6 the_p_versus_np_tuple_that_fails_at_the_5c3959_s6 the_p_versus_np_tuple_that_fails_at_the_5c3959_s7) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l7 the_p_versus_np_tuple_that_fails_at_the_5c3959_s7 the_p_versus_np_tuple_that_fails_at_the_5c3959_s8) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l8 the_p_versus_np_tuple_that_fails_at_the_5c3959_s8 the_p_versus_np_tuple_that_fails_at_the_5c3959_s9) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l9 the_p_versus_np_tuple_that_fails_at_the_5c3959_s9 the_p_versus_np_tuple_that_fails_at_the_5c3959_s10) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l10 the_p_versus_np_tuple_that_fails_at_the_5c3959_s10 the_p_versus_np_tuple_that_fails_at_the_5c3959_s11) (.seq (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l11 the_p_versus_np_tuple_that_fails_at_the_5c3959_s11 the_p_versus_np_tuple_that_fails_at_the_5c3959_s12) (.arrow the_p_versus_np_tuple_that_fails_at_the_5c3959_l12 the_p_versus_np_tuple_that_fails_at_the_5c3959_s12 the_p_versus_np_tuple_that_fails_at_the_5c3959_s13))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_p_versus_np_tuple_that_fails_at_the_5c3959_true_arm : IGProtocol the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s13 :=
  (the_p_versus_np_tuple_that_fails_at_the_5c3959_protocol).restrictToEVALT

-- false arm
noncomputable def the_p_versus_np_tuple_that_fails_at_the_5c3959_false_arm : IGProtocol the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s13 :=
  (the_p_versus_np_tuple_that_fails_at_the_5c3959_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_p_versus_np_tuple_that_fails_at_the_5c3959_tier : OuroboricityTier := TierFunctor.obj the_p_versus_np_tuple_that_fails_at_the_5c3959_s0
#eval the_p_versus_np_tuple_that_fails_at_the_5c3959_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_p_versus_np_tuple_that_fails_at_the_5c3959_frobenius :
    igFrobeniusAlg.mul the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 = the_p_versus_np_tuple_that_fails_at_the_5c3959_s0 :=
  igFrobAlg_self_fusion the_p_versus_np_tuple_that_fails_at_the_5c3959_s0
