-- IGProtocol scaffold: VINIT → TANCH → FSPLIT → AFWD → EVALT → AREV → EVALT → ENGAGR → FFUSE → IMSCRIB → CLINK → IFIX
-- Class: Single therapy instance with natural compound mapping — structurally typed and Frobenius-verified therapy design output, linked to specific Phytoglyphica/Fungiglyphica/Animaglyphica compounds via routing morphisms
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=False | period=12
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(2, 8)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑡  | initial object — ground of distinction
--   [1] TANCH     top    := 𐑡               𐑼 → 𐑚  | terminal object — connectivity boundary
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [6] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [7] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [8] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [9] IMSCRIB   gram   := 𐑠               𐑙 → 𐑱  | identity — self-imscription
--   [10] CLINK     fid    := 𐑱               𐑠 → 𐑭  | composition — regime coherence
--   [11] IFIX      prot   := 𐑭               𐑱 → 𐑼  | irreversible fixation — winding number

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def single_therapy_instance_with_natural_e721cf_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := up, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def single_therapy_instance_with_natural_e721cf_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def single_therapy_instance_with_natural_e721cf_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def single_therapy_instance_with_natural_e721cf_protocol : IGProtocol single_therapy_instance_with_natural_e721cf_s0 single_therapy_instance_with_natural_e721cf_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct single_therapy_instance_with_natural_e721cf_s8 single_therapy_instance_with_natural_e721cf_s8 = single_therapy_instance_with_natural_e721cf_s8 (idempotent)
  (.seq (.arrow single_therapy_instance_with_natural_e721cf_l0 single_therapy_instance_with_natural_e721cf_s0 single_therapy_instance_with_natural_e721cf_s1) (.seq (.arrow single_therapy_instance_with_natural_e721cf_l1 single_therapy_instance_with_natural_e721cf_s1 single_therapy_instance_with_natural_e721cf_s2) (.seq (.prod (.arrow single_therapy_instance_with_natural_e721cf_l2 single_therapy_instance_with_natural_e721cf_s2 single_therapy_instance_with_natural_e721cf_s8) (.arrow single_therapy_instance_with_natural_e721cf_l2 single_therapy_instance_with_natural_e721cf_s2 single_therapy_instance_with_natural_e721cf_s8)) (.seq (.arrow single_therapy_instance_with_natural_e721cf_l8 single_therapy_instance_with_natural_e721cf_s8 single_therapy_instance_with_natural_e721cf_s8) (.seq (.arrow single_therapy_instance_with_natural_e721cf_l8 single_therapy_instance_with_natural_e721cf_s8 single_therapy_instance_with_natural_e721cf_s9) (.seq (.arrow single_therapy_instance_with_natural_e721cf_l9 single_therapy_instance_with_natural_e721cf_s9 single_therapy_instance_with_natural_e721cf_s10) (.arrow single_therapy_instance_with_natural_e721cf_l10 single_therapy_instance_with_natural_e721cf_s10 single_therapy_instance_with_natural_e721cf_s11)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def single_therapy_instance_with_natural_e721cf_true_arm : IGProtocol single_therapy_instance_with_natural_e721cf_s0 single_therapy_instance_with_natural_e721cf_s11 :=
  (single_therapy_instance_with_natural_e721cf_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def single_therapy_instance_with_natural_e721cf_tier : OuroboricityTier := TierFunctor.obj single_therapy_instance_with_natural_e721cf_s0
#eval single_therapy_instance_with_natural_e721cf_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem single_therapy_instance_with_natural_e721cf_frobenius :
    igFrobeniusAlg.mul single_therapy_instance_with_natural_e721cf_s0 single_therapy_instance_with_natural_e721cf_s0 = single_therapy_instance_with_natural_e721cf_s0 :=
  igFrobAlg_self_fusion single_therapy_instance_with_natural_e721cf_s0
