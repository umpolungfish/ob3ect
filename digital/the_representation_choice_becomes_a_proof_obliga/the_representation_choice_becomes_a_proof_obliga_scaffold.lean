-- IGProtocol scaffold: VINIT → IMSCRIB → FSPLIT → EVALT → AREV → FFUSE → CLINK → IFIX → IMSCRIB → AFWD → FSPLIT → EVALT → ENGAGR → FFUSE → TANCH
-- Class: the representation choice becomes a proof obligation that the encoding elaborates without stack overflow AND native_decides within the memory budget, certified by the flat-type invariant that killed the nested-record tower at the cubic layer
-- Fingerprint: sig=(7,4,3,1)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=False | period=15
-- Expected tier: O₀
-- FSPLIT/FFUSE pairs: [(2, 5), (10, 13)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑚  | identity — self-imscription
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [4] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [6] CLINK     fid    := 𐑱               𐑙 → 𐑭  | composition — regime coherence
--   [7] IFIX      prot   := 𐑭               𐑱 → 𐑠  | irreversible fixation — winding number
--   [8] IMSCRIB   gram   := 𐑠               𐑭 → 𐑾  | identity — self-imscription
--   [9] AFWD      rel    := 𐑾               𐑠 → 𐑚  | forward morphism — bidirectional arrow
--   [10] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [11] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [12] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [13] FFUSE     stoi   := 𐑙               𐑙 → 𐑡  | fuse μ — assembly mode
--   [14] TANCH     top    := 𐑡               𐑙 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_representation_choice_becomes_a_ef7ca9_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_s7 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s8 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := up, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_representation_choice_becomes_a_ef7ca9_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_representation_choice_becomes_a_ef7ca9_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l9 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_representation_choice_becomes_a_ef7ca9_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_representation_choice_becomes_a_ef7ca9_protocol : IGProtocol the_representation_choice_becomes_a_ef7ca9_s0 the_representation_choice_becomes_a_ef7ca9_s14 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_representation_choice_becomes_a_ef7ca9_s5 the_representation_choice_becomes_a_ef7ca9_s5 = the_representation_choice_becomes_a_ef7ca9_s5 (idempotent)
  (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l0 the_representation_choice_becomes_a_ef7ca9_s0 the_representation_choice_becomes_a_ef7ca9_s1) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l1 the_representation_choice_becomes_a_ef7ca9_s1 the_representation_choice_becomes_a_ef7ca9_s2) (.seq (.prod (.arrow the_representation_choice_becomes_a_ef7ca9_l2 the_representation_choice_becomes_a_ef7ca9_s2 the_representation_choice_becomes_a_ef7ca9_s5) (.arrow the_representation_choice_becomes_a_ef7ca9_l2 the_representation_choice_becomes_a_ef7ca9_s2 the_representation_choice_becomes_a_ef7ca9_s5)) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l5 the_representation_choice_becomes_a_ef7ca9_s5 the_representation_choice_becomes_a_ef7ca9_s5) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l5 the_representation_choice_becomes_a_ef7ca9_s5 the_representation_choice_becomes_a_ef7ca9_s6) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l6 the_representation_choice_becomes_a_ef7ca9_s6 the_representation_choice_becomes_a_ef7ca9_s7) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l7 the_representation_choice_becomes_a_ef7ca9_s7 the_representation_choice_becomes_a_ef7ca9_s8) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l8 the_representation_choice_becomes_a_ef7ca9_s8 the_representation_choice_becomes_a_ef7ca9_s9) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l9 the_representation_choice_becomes_a_ef7ca9_s9 the_representation_choice_becomes_a_ef7ca9_s10) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l10 the_representation_choice_becomes_a_ef7ca9_s10 the_representation_choice_becomes_a_ef7ca9_s11) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l11 the_representation_choice_becomes_a_ef7ca9_s11 the_representation_choice_becomes_a_ef7ca9_s12) (.seq (.arrow the_representation_choice_becomes_a_ef7ca9_l12 the_representation_choice_becomes_a_ef7ca9_s12 the_representation_choice_becomes_a_ef7ca9_s13) (.arrow the_representation_choice_becomes_a_ef7ca9_l13 the_representation_choice_becomes_a_ef7ca9_s13 the_representation_choice_becomes_a_ef7ca9_s14)))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_representation_choice_becomes_a_ef7ca9_true_arm : IGProtocol the_representation_choice_becomes_a_ef7ca9_s0 the_representation_choice_becomes_a_ef7ca9_s14 :=
  (the_representation_choice_becomes_a_ef7ca9_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₀.
def the_representation_choice_becomes_a_ef7ca9_tier : OuroboricityTier := TierFunctor.obj the_representation_choice_becomes_a_ef7ca9_s0
#eval the_representation_choice_becomes_a_ef7ca9_tier  -- the Grammar's own verdict on its tier
