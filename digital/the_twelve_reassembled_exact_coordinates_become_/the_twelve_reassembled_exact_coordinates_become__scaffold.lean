-- IGProtocol scaffold: VINIT → AFWD → IFIX → AREV → FSPLIT → EVALT → FFUSE → AREV → FSPLIT → EVALF → AREV → ENGAGR → CLINK → IMSCRIB → FFUSE → AFWD → IFIX → TANCH
-- Class: the twelve reassembled exact coordinates become certified as the true fiducial when each, re-evaluated at the distinguished complex embedding of the degree-288 field, reproduces the 1500-digit numeric witness to full precision and passes the certification gate, collapsing the exact algebraic fiducial and the numeric fiducial into a single object
-- Fingerprint: sig=(9,4,3,2)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=True | period=18
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(4, 6), (8, 14)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑭  | forward morphism — bidirectional arrow
--   [2] IFIX      prot   := 𐑭               𐑾 → 𐑗  | irreversible fixation — winding number
--   [3] AREV      pol    := 𐑗               𐑭 → 𐑚  | reverse morphism — parity flip
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] FFUSE     stoi   := 𐑙               𐑙 → 𐑗  | fuse μ — assembly mode
--   [7] AREV      pol    := 𐑗               𐑙 → 𐑚  | reverse morphism — parity flip
--   [8] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [9] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [10] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [11] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [12] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [13] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [14] FFUSE     stoi   := 𐑙               𐑙 → 𐑾  | fuse μ — assembly mode
--   [15] AFWD      rel    := 𐑾               𐑙 → 𐑭  | forward morphism — bidirectional arrow
--   [16] IFIX      prot   := 𐑭               𐑾 → 𐑡  | irreversible fixation — winding number
--   [17] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_twelve_reassembled_exact_f47862_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_twelve_reassembled_exact_f47862_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_twelve_reassembled_exact_f47862_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_twelve_reassembled_exact_f47862_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_s17 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_twelve_reassembled_exact_f47862_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_twelve_reassembled_exact_f47862_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l15 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_twelve_reassembled_exact_f47862_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_twelve_reassembled_exact_f47862_l17 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_twelve_reassembled_exact_f47862_protocol : IGProtocol the_twelve_reassembled_exact_f47862_s0 the_twelve_reassembled_exact_f47862_s17 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_twelve_reassembled_exact_f47862_s6 the_twelve_reassembled_exact_f47862_s6 = the_twelve_reassembled_exact_f47862_s6 (idempotent)
  (.seq (.arrow the_twelve_reassembled_exact_f47862_l0 the_twelve_reassembled_exact_f47862_s0 the_twelve_reassembled_exact_f47862_s1) (.seq (.arrow the_twelve_reassembled_exact_f47862_l1 the_twelve_reassembled_exact_f47862_s1 the_twelve_reassembled_exact_f47862_s2) (.seq (.arrow the_twelve_reassembled_exact_f47862_l2 the_twelve_reassembled_exact_f47862_s2 the_twelve_reassembled_exact_f47862_s3) (.seq (.arrow the_twelve_reassembled_exact_f47862_l3 the_twelve_reassembled_exact_f47862_s3 the_twelve_reassembled_exact_f47862_s4) (.seq (.prod (.arrow the_twelve_reassembled_exact_f47862_l4 the_twelve_reassembled_exact_f47862_s4 the_twelve_reassembled_exact_f47862_s6) (.arrow the_twelve_reassembled_exact_f47862_l4 the_twelve_reassembled_exact_f47862_s4 the_twelve_reassembled_exact_f47862_s6)) (.seq (.arrow the_twelve_reassembled_exact_f47862_l6 the_twelve_reassembled_exact_f47862_s6 the_twelve_reassembled_exact_f47862_s6) (.seq (.arrow the_twelve_reassembled_exact_f47862_l6 the_twelve_reassembled_exact_f47862_s6 the_twelve_reassembled_exact_f47862_s7) (.seq (.arrow the_twelve_reassembled_exact_f47862_l7 the_twelve_reassembled_exact_f47862_s7 the_twelve_reassembled_exact_f47862_s8) (.seq (.arrow the_twelve_reassembled_exact_f47862_l8 the_twelve_reassembled_exact_f47862_s8 the_twelve_reassembled_exact_f47862_s9) (.seq (.arrow the_twelve_reassembled_exact_f47862_l9 the_twelve_reassembled_exact_f47862_s9 the_twelve_reassembled_exact_f47862_s10) (.seq (.arrow the_twelve_reassembled_exact_f47862_l10 the_twelve_reassembled_exact_f47862_s10 the_twelve_reassembled_exact_f47862_s11) (.seq (.arrow the_twelve_reassembled_exact_f47862_l11 the_twelve_reassembled_exact_f47862_s11 the_twelve_reassembled_exact_f47862_s12) (.seq (.arrow the_twelve_reassembled_exact_f47862_l12 the_twelve_reassembled_exact_f47862_s12 the_twelve_reassembled_exact_f47862_s13) (.seq (.arrow the_twelve_reassembled_exact_f47862_l13 the_twelve_reassembled_exact_f47862_s13 the_twelve_reassembled_exact_f47862_s14) (.seq (.arrow the_twelve_reassembled_exact_f47862_l14 the_twelve_reassembled_exact_f47862_s14 the_twelve_reassembled_exact_f47862_s15) (.seq (.arrow the_twelve_reassembled_exact_f47862_l15 the_twelve_reassembled_exact_f47862_s15 the_twelve_reassembled_exact_f47862_s16) (.arrow the_twelve_reassembled_exact_f47862_l16 the_twelve_reassembled_exact_f47862_s16 the_twelve_reassembled_exact_f47862_s17)))))))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_twelve_reassembled_exact_f47862_true_arm : IGProtocol the_twelve_reassembled_exact_f47862_s0 the_twelve_reassembled_exact_f47862_s17 :=
  (the_twelve_reassembled_exact_f47862_protocol).restrictToEVALT

-- false arm
noncomputable def the_twelve_reassembled_exact_f47862_false_arm : IGProtocol the_twelve_reassembled_exact_f47862_s0 the_twelve_reassembled_exact_f47862_s17 :=
  (the_twelve_reassembled_exact_f47862_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_twelve_reassembled_exact_f47862_tier : OuroboricityTier := TierFunctor.obj the_twelve_reassembled_exact_f47862_s0
#eval the_twelve_reassembled_exact_f47862_tier  -- the Grammar's own verdict on its tier
