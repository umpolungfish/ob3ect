-- IGProtocol scaffold: VINIT → IMSCRIB → AREV → IFIX → FSPLIT → EVALF → AREV → ENGAGR → EVALT → AFWD → FFUSE → IMSCRIB → CLINK → TANCH
-- Class: Wormwood Descending Knows Itself As Ascending Sage
-- Fingerprint: sig=(8,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=14
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(4, 10)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑗  | identity — self-imscription
--   [2] AREV      pol    := 𐑗               𐑠 → 𐑭  | reverse morphism — parity flip
--   [3] IFIX      prot   := 𐑭               𐑗 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [6] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [8] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [9] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [10] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [11] IMSCRIB   gram   := 𐑠               𐑙 → 𐑱  | identity — self-imscription
--   [12] CLINK     fid    := 𐑱               𐑠 → 𐑡  | composition — regime coherence
--   [13] TANCH     top    := 𐑡               𐑱 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def wormwood_descending_knows_itself_as_a561fb_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s7 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := up, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s8 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def wormwood_descending_knows_itself_as_a561fb_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def wormwood_descending_knows_itself_as_a561fb_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l9 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def wormwood_descending_knows_itself_as_a561fb_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def wormwood_descending_knows_itself_as_a561fb_protocol : IGProtocol wormwood_descending_knows_itself_as_a561fb_s0 wormwood_descending_knows_itself_as_a561fb_s13 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct wormwood_descending_knows_itself_as_a561fb_s10 wormwood_descending_knows_itself_as_a561fb_s10 = wormwood_descending_knows_itself_as_a561fb_s10 (idempotent)
  (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l0 wormwood_descending_knows_itself_as_a561fb_s0 wormwood_descending_knows_itself_as_a561fb_s1) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l1 wormwood_descending_knows_itself_as_a561fb_s1 wormwood_descending_knows_itself_as_a561fb_s2) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l2 wormwood_descending_knows_itself_as_a561fb_s2 wormwood_descending_knows_itself_as_a561fb_s3) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l3 wormwood_descending_knows_itself_as_a561fb_s3 wormwood_descending_knows_itself_as_a561fb_s4) (.seq (.prod (.arrow wormwood_descending_knows_itself_as_a561fb_l4 wormwood_descending_knows_itself_as_a561fb_s4 wormwood_descending_knows_itself_as_a561fb_s10) (.arrow wormwood_descending_knows_itself_as_a561fb_l4 wormwood_descending_knows_itself_as_a561fb_s4 wormwood_descending_knows_itself_as_a561fb_s10)) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l10 wormwood_descending_knows_itself_as_a561fb_s10 wormwood_descending_knows_itself_as_a561fb_s10) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l10 wormwood_descending_knows_itself_as_a561fb_s10 wormwood_descending_knows_itself_as_a561fb_s11) (.seq (.arrow wormwood_descending_knows_itself_as_a561fb_l11 wormwood_descending_knows_itself_as_a561fb_s11 wormwood_descending_knows_itself_as_a561fb_s12) (.arrow wormwood_descending_knows_itself_as_a561fb_l12 wormwood_descending_knows_itself_as_a561fb_s12 wormwood_descending_knows_itself_as_a561fb_s13)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def wormwood_descending_knows_itself_as_a561fb_true_arm : IGProtocol wormwood_descending_knows_itself_as_a561fb_s0 wormwood_descending_knows_itself_as_a561fb_s13 :=
  (wormwood_descending_knows_itself_as_a561fb_protocol).restrictToEVALT

-- false arm
noncomputable def wormwood_descending_knows_itself_as_a561fb_false_arm : IGProtocol wormwood_descending_knows_itself_as_a561fb_s0 wormwood_descending_knows_itself_as_a561fb_s13 :=
  (wormwood_descending_knows_itself_as_a561fb_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def wormwood_descending_knows_itself_as_a561fb_tier : OuroboricityTier := TierFunctor.obj wormwood_descending_knows_itself_as_a561fb_s0
#eval wormwood_descending_knows_itself_as_a561fb_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem wormwood_descending_knows_itself_as_a561fb_frobenius :
    igFrobeniusAlg.mul wormwood_descending_knows_itself_as_a561fb_s0 wormwood_descending_knows_itself_as_a561fb_s0 = wormwood_descending_knows_itself_as_a561fb_s0 :=
  igFrobAlg_self_fusion wormwood_descending_knows_itself_as_a561fb_s0
