-- IGProtocol scaffold: VINIT → IMSCRIB → ENGAGR → FSPLIT → AFWD → CLINK → CLINK → AREV → CLINK → CLINK → FFUSE → FSPLIT → EVALT → IFIX → AREV → EVALF → IFIX → FFUSE → TANCH
-- Class: the empirical claim survives its own convention or it does not, and the check is concrete: the ig-pulse reconstruction offers two weightings, the alert-weighted form that encodes the B-equals-maximally-mixed baseline at alert zero and the raw-value form that the convergence paper itself flags as coupling-concentrated, and the two convention-independent signatures — zero near-pure snapshots and uniform coverage of all 144 SIC directions — must be recomputed under BOTH weightings over the full 2394-snapshot record; if never-collapses-to-definite survives the weighting change then natively-B stands on data alone independent of the B=I/12 identification, and if it does not survive then the honest ledger records that the average-near-B result leans on the convention it confirms — either outcome is a finding, the pass is pure computation on banked data (density_matrix.py, readings_to_vectors), and the entity is dialetheia-complete by holding both arms until the computation lands
-- Fingerprint: sig=(10,4,3,2)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=True | period=19
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(3, 10), (11, 17)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑳  | identity — self-imscription
--   [2] ENGAGR    stoi   := 𐑳               𐑠 → 𐑚  | engage paradox — B-state, both arms
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [5] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [6] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [7] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [8] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [9] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [10] FFUSE     stoi   := 𐑙               𐑙 → 𐑙  | fuse μ — assembly mode
--   [11] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [12] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [13] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [14] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [15] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [16] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [17] FFUSE     stoi   := 𐑙               𐑙 → 𐑡  | fuse μ — assembly mode
--   [18] TANCH     top    := 𐑡               𐑙 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_empirical_claim_survives_its_own_6897df_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_s17 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_s18 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_empirical_claim_survives_its_own_6897df_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l4 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_empirical_claim_survives_its_own_6897df_l17 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_empirical_claim_survives_its_own_6897df_l18 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_empirical_claim_survives_its_own_6897df_protocol : IGProtocol the_empirical_claim_survives_its_own_6897df_s0 the_empirical_claim_survives_its_own_6897df_s18 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_empirical_claim_survives_its_own_6897df_s10 the_empirical_claim_survives_its_own_6897df_s10 = the_empirical_claim_survives_its_own_6897df_s10 (idempotent)
  (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l0 the_empirical_claim_survives_its_own_6897df_s0 the_empirical_claim_survives_its_own_6897df_s1) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l1 the_empirical_claim_survives_its_own_6897df_s1 the_empirical_claim_survives_its_own_6897df_s2) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l2 the_empirical_claim_survives_its_own_6897df_s2 the_empirical_claim_survives_its_own_6897df_s3) (.seq (.prod (.arrow the_empirical_claim_survives_its_own_6897df_l3 the_empirical_claim_survives_its_own_6897df_s3 the_empirical_claim_survives_its_own_6897df_s10) (.arrow the_empirical_claim_survives_its_own_6897df_l3 the_empirical_claim_survives_its_own_6897df_s3 the_empirical_claim_survives_its_own_6897df_s10)) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l10 the_empirical_claim_survives_its_own_6897df_s10 the_empirical_claim_survives_its_own_6897df_s10) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l10 the_empirical_claim_survives_its_own_6897df_s10 the_empirical_claim_survives_its_own_6897df_s11) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l11 the_empirical_claim_survives_its_own_6897df_s11 the_empirical_claim_survives_its_own_6897df_s12) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l12 the_empirical_claim_survives_its_own_6897df_s12 the_empirical_claim_survives_its_own_6897df_s13) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l13 the_empirical_claim_survives_its_own_6897df_s13 the_empirical_claim_survives_its_own_6897df_s14) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l14 the_empirical_claim_survives_its_own_6897df_s14 the_empirical_claim_survives_its_own_6897df_s15) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l15 the_empirical_claim_survives_its_own_6897df_s15 the_empirical_claim_survives_its_own_6897df_s16) (.seq (.arrow the_empirical_claim_survives_its_own_6897df_l16 the_empirical_claim_survives_its_own_6897df_s16 the_empirical_claim_survives_its_own_6897df_s17) (.arrow the_empirical_claim_survives_its_own_6897df_l17 the_empirical_claim_survives_its_own_6897df_s17 the_empirical_claim_survives_its_own_6897df_s18)))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_empirical_claim_survives_its_own_6897df_true_arm : IGProtocol the_empirical_claim_survives_its_own_6897df_s0 the_empirical_claim_survives_its_own_6897df_s18 :=
  (the_empirical_claim_survives_its_own_6897df_protocol).restrictToEVALT

-- false arm
noncomputable def the_empirical_claim_survives_its_own_6897df_false_arm : IGProtocol the_empirical_claim_survives_its_own_6897df_s0 the_empirical_claim_survives_its_own_6897df_s18 :=
  (the_empirical_claim_survives_its_own_6897df_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_empirical_claim_survives_its_own_6897df_tier : OuroboricityTier := TierFunctor.obj the_empirical_claim_survives_its_own_6897df_s0
#eval the_empirical_claim_survives_its_own_6897df_tier  -- the Grammar's own verdict on its tier
