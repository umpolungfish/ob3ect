-- IGProtocol scaffold: VINIT → AFWD → IMSCRIB → FSPLIT → EVALF → IFIX → AREV → AREV → EVALT → IFIX → CLINK → ENGAGR → FFUSE → IMSCRIB → TANCH
-- Class: the crux phase u_1, whose genuine degree-32 minimal polynomial is a palindromic polynomial in x to the fourth, becomes the fourth root of one half of c plus i root four minus c squared where c is the real root of the solvable quartic nine x to the fourth minus three-hundred-sixty-eight x cubed plus six-hundred-thirty-two x squared plus nine-hundred-sixty x minus one-thousand-three-hundred-ninety-two of Galois group D4, so that z_1 equals the square root of its pinned S-unit modulus times that phase reproduces the fiducial to full precision
-- Fingerprint: sig=(8,2,3,2)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=15
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(3, 12)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] AFWD      rel    := 𐑾               𐑼 → 𐑠  | forward morphism — bidirectional arrow
--   [2] IMSCRIB   gram   := 𐑠               𐑾 → 𐑚  | identity — self-imscription
--   [3] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [4] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [5] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [6] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [8] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [9] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [10] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [11] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [12] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [13] IMSCRIB   gram   := 𐑠               𐑙 → 𐑡  | identity — self-imscription
--   [14] TANCH     top    := 𐑡               𐑠 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_crux_phase_u_1_whose_genuine_degree_de0f0d_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_crux_phase_u_1_whose_genuine_degree_de0f0d_protocol : IGProtocol the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s14 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 = the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 (idempotent)
  (.seq (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s1) (.seq (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l1 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s1 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s2) (.seq (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l2 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s2 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s3) (.seq (.prod (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l3 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s3 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12) (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l3 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s3 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12)) (.seq (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l12 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12) (.seq (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l12 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s12 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s13) (.arrow the_crux_phase_u_1_whose_genuine_degree_de0f0d_l13 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s13 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s14)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_crux_phase_u_1_whose_genuine_degree_de0f0d_true_arm : IGProtocol the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s14 :=
  (the_crux_phase_u_1_whose_genuine_degree_de0f0d_protocol).restrictToEVALT

-- false arm
noncomputable def the_crux_phase_u_1_whose_genuine_degree_de0f0d_false_arm : IGProtocol the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s14 :=
  (the_crux_phase_u_1_whose_genuine_degree_de0f0d_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_crux_phase_u_1_whose_genuine_degree_de0f0d_tier : OuroboricityTier := TierFunctor.obj the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0
#eval the_crux_phase_u_1_whose_genuine_degree_de0f0d_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_crux_phase_u_1_whose_genuine_degree_de0f0d_frobenius :
    igFrobeniusAlg.mul the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 = the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0 :=
  igFrobAlg_self_fusion the_crux_phase_u_1_whose_genuine_degree_de0f0d_s0
