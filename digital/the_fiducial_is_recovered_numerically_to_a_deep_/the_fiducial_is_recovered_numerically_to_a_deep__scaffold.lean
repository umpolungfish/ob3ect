-- IGProtocol scaffold: VINIT → FSPLIT → EVALT → AFWD → AREV → EVALF → FFUSE → IMSCRIB → CLINK → AFWD → AREV → IMSCRIB → CLINK → ENGAGR → FSPLIT → EVALT → IFIX → EVALF → FFUSE → TANCH
-- Class: the fiducial is recovered numerically to a deep floor as the seed for algebraic recognition, exactly as the d equals twelve fiducial was flowed to fifteen hundred digits: solve the full overlap system by Gauss-Newton or the standard SIC flow inside the Zauner-covariant subspace so only the reduced degrees of freedom are carried, seeding from the Appleby-Grassl exact-SIC data if a two thousand forty eight or two-power-tower fiducial already exists there and flowing it from a Weyl-Heisenberg fiducial ansatz otherwise, driving the residual of every orbit-representative overlap below the deep-vanish floor so that subsequent algebraic-dependence recognition of the moduli and phases has a certificate to floor against rather than a shallow creep to mistake for a relation, with the honest note that even this recovery is a real computation at d equals two thousand forty eight and its success or failure is itself the first finding
-- Fingerprint: sig=(10,4,5,1)
--   self_ref=False | frobenius_order=3
--   dialetheia_complete=True | period=20
-- Expected tier: O₁
-- FSPLIT/FFUSE pairs: [(1, 6), (14, 18)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑚  | initial object — ground of distinction
--   [1] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [2] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [3] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [6] FFUSE     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [7] IMSCRIB   gram   := 𐑠               𐑙 → 𐑱  | identity — self-imscription
--   [8] CLINK     fid    := 𐑱               𐑠 → 𐑾  | composition — regime coherence
--   [9] AFWD      rel    := 𐑾               𐑱 → 𐑗  | forward morphism — bidirectional arrow
--   [10] AREV      pol    := 𐑗               𐑾 → 𐑠  | reverse morphism — parity flip
--   [11] IMSCRIB   gram   := 𐑠               𐑗 → 𐑱  | identity — self-imscription
--   [12] CLINK     fid    := 𐑱               𐑠 → 𐑳  | composition — regime coherence
--   [13] ENGAGR    stoi   := 𐑳               𐑱 → 𐑚  | engage paradox — B-state, both arms
--   [14] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [15] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [16] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [17] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [18] FFUSE     stoi   := 𐑙               𐑙 → 𐑡  | fuse μ — assembly mode
--   [19] TANCH     top    := 𐑡               𐑙 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_fiducial_is_recovered_numerically_26d494_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_s16 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_fiducial_is_recovered_numerically_26d494_s17 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_fiducial_is_recovered_numerically_26d494_s18 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_fiducial_is_recovered_numerically_26d494_s19 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_fiducial_is_recovered_numerically_26d494_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l9 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l16 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_fiducial_is_recovered_numerically_26d494_l17 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l18 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_fiducial_is_recovered_numerically_26d494_l19 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_fiducial_is_recovered_numerically_26d494_protocol : IGProtocol the_fiducial_is_recovered_numerically_26d494_s0 the_fiducial_is_recovered_numerically_26d494_s19 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_fiducial_is_recovered_numerically_26d494_s6 the_fiducial_is_recovered_numerically_26d494_s6 = the_fiducial_is_recovered_numerically_26d494_s6 (idempotent)
  (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l0 the_fiducial_is_recovered_numerically_26d494_s0 the_fiducial_is_recovered_numerically_26d494_s1) (.seq (.prod (.arrow the_fiducial_is_recovered_numerically_26d494_l1 the_fiducial_is_recovered_numerically_26d494_s1 the_fiducial_is_recovered_numerically_26d494_s6) (.arrow the_fiducial_is_recovered_numerically_26d494_l1 the_fiducial_is_recovered_numerically_26d494_s1 the_fiducial_is_recovered_numerically_26d494_s6)) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l6 the_fiducial_is_recovered_numerically_26d494_s6 the_fiducial_is_recovered_numerically_26d494_s6) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l6 the_fiducial_is_recovered_numerically_26d494_s6 the_fiducial_is_recovered_numerically_26d494_s7) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l7 the_fiducial_is_recovered_numerically_26d494_s7 the_fiducial_is_recovered_numerically_26d494_s8) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l8 the_fiducial_is_recovered_numerically_26d494_s8 the_fiducial_is_recovered_numerically_26d494_s9) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l9 the_fiducial_is_recovered_numerically_26d494_s9 the_fiducial_is_recovered_numerically_26d494_s10) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l10 the_fiducial_is_recovered_numerically_26d494_s10 the_fiducial_is_recovered_numerically_26d494_s11) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l11 the_fiducial_is_recovered_numerically_26d494_s11 the_fiducial_is_recovered_numerically_26d494_s12) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l12 the_fiducial_is_recovered_numerically_26d494_s12 the_fiducial_is_recovered_numerically_26d494_s13) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l13 the_fiducial_is_recovered_numerically_26d494_s13 the_fiducial_is_recovered_numerically_26d494_s14) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l14 the_fiducial_is_recovered_numerically_26d494_s14 the_fiducial_is_recovered_numerically_26d494_s15) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l15 the_fiducial_is_recovered_numerically_26d494_s15 the_fiducial_is_recovered_numerically_26d494_s16) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l16 the_fiducial_is_recovered_numerically_26d494_s16 the_fiducial_is_recovered_numerically_26d494_s17) (.seq (.arrow the_fiducial_is_recovered_numerically_26d494_l17 the_fiducial_is_recovered_numerically_26d494_s17 the_fiducial_is_recovered_numerically_26d494_s18) (.arrow the_fiducial_is_recovered_numerically_26d494_l18 the_fiducial_is_recovered_numerically_26d494_s18 the_fiducial_is_recovered_numerically_26d494_s19))))))))))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_fiducial_is_recovered_numerically_26d494_true_arm : IGProtocol the_fiducial_is_recovered_numerically_26d494_s0 the_fiducial_is_recovered_numerically_26d494_s19 :=
  (the_fiducial_is_recovered_numerically_26d494_protocol).restrictToEVALT

-- false arm
noncomputable def the_fiducial_is_recovered_numerically_26d494_false_arm : IGProtocol the_fiducial_is_recovered_numerically_26d494_s0 the_fiducial_is_recovered_numerically_26d494_s19 :=
  (the_fiducial_is_recovered_numerically_26d494_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₁.
def the_fiducial_is_recovered_numerically_26d494_tier : OuroboricityTier := TierFunctor.obj the_fiducial_is_recovered_numerically_26d494_s0
#eval the_fiducial_is_recovered_numerically_26d494_tier  -- the Grammar's own verdict on its tier
