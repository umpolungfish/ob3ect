-- IGProtocol scaffold: VINIT → IMSCRIB → AFWD → IFIX → FSPLIT → CLINK → AFWD → IMSCRIB → EVALT → IFIX → AREV → EVALF → FFUSE → ENGAGR → IFIX → TANCH
-- Class: the 143 Weyl-Heisenberg equiangularity conditions become 143 decidable overlap-squared-equals-one-thirteenth identities proved by native_decide in the computable cyclotomic ring rather than in the complex numbers
-- Fingerprint: sig=(8,2,3,3)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=16
-- Expected tier: O₂
-- FSPLIT/FFUSE pairs: [(4, 12)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑾  | identity — self-imscription
--   [2] AFWD      rel    := 𐑾               𐑠 → 𐑭  | forward morphism — bidirectional arrow
--   [3] IFIX      prot   := 𐑭               𐑾 → 𐑚  | irreversible fixation — winding number
--   [4] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [5] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [6] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [7] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [8] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [9] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [10] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [11] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [12] FFUSE     stoi   := 𐑙               𐑙 → 𐑳  | fuse μ — assembly mode
--   [13] ENGAGR    stoi   := 𐑳               𐑙 → 𐑭  | engage paradox — B-state, both arms
--   [14] IFIX      prot   := 𐑭               𐑳 → 𐑡  | irreversible fixation — winding number
--   [15] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s14 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_s15 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l2 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l6 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l14 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def the_143_weyl_heisenberg_equiangularity_f8f4cf_l15 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def the_143_weyl_heisenberg_equiangularity_f8f4cf_protocol : IGProtocol the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s15 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 = the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 (idempotent)
  (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s1) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l1 the_143_weyl_heisenberg_equiangularity_f8f4cf_s1 the_143_weyl_heisenberg_equiangularity_f8f4cf_s2) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l2 the_143_weyl_heisenberg_equiangularity_f8f4cf_s2 the_143_weyl_heisenberg_equiangularity_f8f4cf_s3) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l3 the_143_weyl_heisenberg_equiangularity_f8f4cf_s3 the_143_weyl_heisenberg_equiangularity_f8f4cf_s4) (.seq (.prod (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l4 the_143_weyl_heisenberg_equiangularity_f8f4cf_s4 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12) (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l4 the_143_weyl_heisenberg_equiangularity_f8f4cf_s4 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12)) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l12 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l12 the_143_weyl_heisenberg_equiangularity_f8f4cf_s12 the_143_weyl_heisenberg_equiangularity_f8f4cf_s13) (.seq (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l13 the_143_weyl_heisenberg_equiangularity_f8f4cf_s13 the_143_weyl_heisenberg_equiangularity_f8f4cf_s14) (.arrow the_143_weyl_heisenberg_equiangularity_f8f4cf_l14 the_143_weyl_heisenberg_equiangularity_f8f4cf_s14 the_143_weyl_heisenberg_equiangularity_f8f4cf_s15)))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def the_143_weyl_heisenberg_equiangularity_f8f4cf_true_arm : IGProtocol the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s15 :=
  (the_143_weyl_heisenberg_equiangularity_f8f4cf_protocol).restrictToEVALT

-- false arm
noncomputable def the_143_weyl_heisenberg_equiangularity_f8f4cf_false_arm : IGProtocol the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s15 :=
  (the_143_weyl_heisenberg_equiangularity_f8f4cf_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). Fingerprint heuristic suggested .O₂.
def the_143_weyl_heisenberg_equiangularity_f8f4cf_tier : OuroboricityTier := TierFunctor.obj the_143_weyl_heisenberg_equiangularity_f8f4cf_s0
#eval the_143_weyl_heisenberg_equiangularity_f8f4cf_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem the_143_weyl_heisenberg_equiangularity_f8f4cf_frobenius :
    igFrobeniusAlg.mul the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 = the_143_weyl_heisenberg_equiangularity_f8f4cf_s0 :=
  igFrobAlg_self_fusion the_143_weyl_heisenberg_equiangularity_f8f4cf_s0
