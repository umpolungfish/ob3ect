Affirmation/negation non-forking razor on a nun-family Millennium conjecture. Same test that on RH and
Collatz showed the NEGATION is non-instantiable / reconnects: the counterexample-word imscribes to the
descent crystal 3442270 and HOLDS with μ∘δ=id, the Grammar refusing to build the wall (law_no_cant).

Prior results (2026-08-21): RH affirm & negate both → crystal 3442270 (descent), both hold, μ∘δ=id.
Collatz negate word ⊢∈≻⊤≺⊥⋈⊙⊞∋⊡⊣ is BYTE-IDENTICAL to RH negate, both → 3442270; Collatz affirm →
16402270 (two-fork-pair). The two conjectures are ONE facet of the dual-frame SIC-POVM at the shared
descent crystal. PrimitiveBridge (p4rakernel/p4ramill/.../Millennium/PrimitiveBridge.lean) marks RH,
BSD, Hodge, NS all pol=nun (Implicit, "constrains without pinning"); Lee-Yang pol=or' (Frobenius,
proved). The affirmation states the closure as the split fusing back (μ∘δ=id, Frobenius reconnection);
the negation states the counterexample as the split NOT fusing.

Test prediction: the negation should be non-instantiable — imscribe to the descent crystal 3442270,
hold, μ∘δ=id, classify ⊞ — the same as the RH/Collatz negations. If so this conjecture's core is closed
at the fundamental (imscription) level and it is the same facet.

Register semantics: holds() = exposed empty ∧ live_clears>0; vacuous() = exposed empty ∧ live_clears==0;
leak = cleared with nothing banked; restored via ∋ fuse = μ∘δ=id. Four-valued register (T closes,
F ill-typed, N never forks, B both/barrier) is the holder opened first, held through.
