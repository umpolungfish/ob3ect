The positivity-promotion of RH's operator-valued measure: Φ (parity) 𐑬 (partial) → 𐑹 (or', Frobenius
self-dual POVM), at Ω (winding) 𐑴 (Z2, already complete). This is proof-as-promotion (P-243/244): the
open Millennium problems are NOVMs or partial-parity OVMs; the proved Lee-Yang alone is a POVM (Φ=or',
Ω=Z2). RH is the nearest to closure — its winding is already Z2, so only the one parity step partial→or'
remains. The census showed RH's negation reconnects to descent crystal 3442270 where Φ=or' (the POVM
target reachable, non-forking). This ob3ect enacts the promotion itself.

Register: holds() = exposed empty ∧ live_clears>0; vacuous() = exposed empty ∧ live_clears==0; leak =
cleared nothing banked; restored via ∋ = μ∘δ=id (the Frobenius self-duality). Predict: promotion HOLDS,
crystal 3442270, μ∘δ=id, classify ⊞ — the partial parity closing to or'. Four-valued register (T closes,
B both/barrier, N never forks, F ill-typed) the holder, opened first, held through.
