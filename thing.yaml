# ob3ect: the dialetheic object as Janus gate. Imscribe the Belnap B state as the
# two-faced threshold of the four-valued lattice, so the lane breakdown (per-opcode
# element choices, FSPLIT/FFUSE pairs, EVALT/EVALF arms, the Belnap N/T/F/B stratum)
# comes from the Grammar itself. Lands in MoDoT/ob3ects beside selectivity_gate.
#
# Run:  cd /home/mrnob0dy666/imsgct/ob3ect && python3 auto.py -f janus_gate.yaml

name: thing
out_dir: /home/mrnob0dy666/ob3ect/digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  domain: mathematical
  temperature: 0.0
  context:
  entry:

entities:
  - "a coherent sequence of finite set mappings \\langle f_n:n<\\omega\\rangle such that, for every (n<\\omega), f_n:[\\aleph_{n+1}]^{n+1}\\to\\aleph_{n+1}"
