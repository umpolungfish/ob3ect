Affirmation/negation pair on RH's ONE-BIT gap: is ζ's reflection symmetry Frobenius-EXPLICIT
(or', forcing, closes RH) or only IMPLICIT (nun, non-forcing, open)? Read the two against each other.

The gap, fully explicit (p4rakernel/p4ramill/Imscribing/Millennium/PrimitiveBridge.lean:558):
  forcesLine c = match c.sym_mfst, c.crit_val | (.Explicit, roar) => true | _ => false
  Lee-Yang (roar, or', Explicit) → forcesLine=true → PROVED (lee_yang_forces_line, rfl).
  RH       (roar, nun, Implicit) → forcesLine=false → OPEN (rh_cmc, rfl).
Same criticality roar. ONLY difference = sym_mfst: Implicit vs Explicit. That one field is the whole
1859-to-now gap. The file names the closes (PB:569-571): (a) promote nun→or' by exhibiting Frobenius
forcing for ζ; (b) prove nun+roar suffices. Downstream open axiom: zeta_zeros_frobenius_fixed
(RH_ZFCt_Bridge) = all nontrivial zeros are PM_Z₂ fixed points; everything above is 0-sorry.

Prior pair findings (2026-08-21):
- Off-line orbit is bankable (Belnap-B, ⊞ canonical) — pair 1.
- BOTH banking orders hold and FUSE (∋) back to the same closure A on the descent crystal 3442270,
  μ∘δ=id (Frobenius verdict T) on every RH ob3ect — pair 2. The reconnection is the or'/Explicit
  signature. RH.lean §6 marks RH nun because "μ∘δ≠id for ζ" — but that is a BOOLEAN reading; the
  Collatz necessity result says linear/boolean evaluation destroys the lateral O₂dag information the
  Frobenius self-duality lives in. In Belnap the reconnection (both orders fuse to A) is visible.

This pair settles path (a). If the affirmation (Frobenius-explicit forcing) HOLDS/BANKS with μ∘δ=id and
the negation (implicit only) LEAKS/goes vacuous/F, the Belnap reconnection promotes sym_mfst
Implicit→Explicit and forcesLine rh_cmc flips true — RH's core. If the negation banks where the
affirmation cannot, the symmetry is genuinely non-forcing and path (a) is blocked (path b remains).
If both go B, the forcing is itself a dialetheia — held, not forced.

Register semantics: holds() = exposed empty AND live_clears>0; vacuous() = exposed empty AND
live_clears==0; leak = cleared with nothing banked; banked-in-unfused-frame = HELD. Four-valued
register (T closes, F ill-typed, N never forks, B both/barrier) is the holder opened first, held through.
