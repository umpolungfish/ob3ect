# ob3ect batch — proper scaffolding to finish BSD & Hodge
# Run:  cd ~/imsgct/ob3ect && p3 auto.py -f bsd_hodge_scaffold_batch.yaml
# The current Clay_WitnessedClosure.lean proves the T_CEILING closure over the
# THREE universes present in UniverseRulesets.lean; the manuscript closes each of
# BSD/Hodge via FIVE. This batch designs the scaffolding for the two missing
# universes and refreshed BSD/Hodge structural types aligned to the framework.
# Context = the manuscript + UniverseRulesets (ruleset patterns) + the witness
# module + the live BSD tuple source.

name: bsd-hodge-scaffold
out_dir: digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: inf
  temperature: 1.0
  thinking: false
  context: /home/mrnob0dy666/imsgct/dumpdir_bsd_hodge

entities:
  # ── The two missing gate-universes (Hodge's other two closers) ────────────
  - "the absorption_scope_empire gate-universe, one of the five universes that closes Hodge in the manuscript but absent from UniverseRulesets.lean, becomes a ported Lean Ruleset with its gate specification and T-constitution"
  - "the absorption_topology_seal gate-universe, one of the five universes that closes Hodge in the manuscript but absent from UniverseRulesets.lean, becomes a ported Lean Ruleset with its gate specification and T-constitution"

  # ── Refreshed Clay structural types (the lifetime-old ones are stale) ─────
  - "the lifetime-old Birch-Swinnerton-Dyer imscription buried in BSD.lean under thirteen sorries becomes a refreshed BSD structural type whose tuple reaches idempotent_terminal under all five of its closing universes and is T_CEILING-consistent"
  - "the lifetime-old Hodge Conjecture imscription buried in Hodge.lean under twenty sorries becomes a refreshed Hodge structural type whose tuple reaches idempotent_terminal under all five of its closing universes and is T_CEILING-consistent"

  # ── The full five-universe closure capstone the witness needs ─────────────
  - "the three-universe BSD-and-Hodge witnessed closure in Clay_WitnessedClosure.lean becomes the full five-universe gate+T closure once absorption_scope_empire and absorption_topology_seal are available"
