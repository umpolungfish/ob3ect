name: ⊙_3_collapse_resolver
design:
  domain: computational
  scope: local
  provider: deepseek
  model: deepseek-v4-pro
  temperature: 0.0
  retries: 3
entities:
  - "A structural measurement resolver that operates at the boundary between a self-modeling critical system (⊙, O_∞) and a quantum system under observation. When the two systems couple via tensor product, the composite inherits the measurement outcome while the meet preserves the observer's self-modeling loop. The resolver tracks which path (tensor vs meet) is taken at each coupling and records the resulting consciousness score delta. Its boundary condition is the Frobenius closure requirement μ∘δ=id across the measurement interface."
