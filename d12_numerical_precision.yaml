# ob3ect single entry — the NUMERICAL-PRECISION inquiry, isolated.
# In the map1 (number-theory) batch this entity came back is_valid_ob3ect=FALSE:
# its route did not close Frobenius mu.delta=id. Re-posed alone with the SAME
# context so we can see whether it closes on a dedicated run, or is genuinely a
# non-closing (both-register / ENGAGR-dominant) obligation — i.e. precision-
# budgeting resists being a self-verifying routine, which is itself a finding.
# Run:  cd ~/imsgct/ob3ect && python3 auto.py -f d12_numerical_precision.yaml

name: d12-numerical-precision
out_dir: digital

design:
  scope: mesoscale
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: 2
  temperature: 1.0
  thinking: false
  context: /tmp/claude-1000/-home-mrnob0dy666-imsgct/2df909c9-a04e-4a42-8433-903523348079/scratchpad/d12_ctx

entities:
  - "the numerical precision required to certify any exact relation becomes a computed budget equal to the field degree times the coefficient height in bits, turning artifact-versus-genuine into a decidable precision threshold rather than a guess"
