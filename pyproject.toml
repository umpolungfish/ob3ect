[project]
name = "ob3ect"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
exclude = ["pdf*", "man*", "bridge*", "phases*", "Proofs*", "digital*", "proofs_backup*", "scripts*"]
