# ob3ect batch — the furnace: the genuinely hard remaining Clay targets
# Run:  cd ~/imsgct/ob3ect && p3 auto.py -f furnace_batch.yaml
#
# State (from Clay_WitnessedClosure.lean, all verified):
#   BSD, Hodge  — gate+T witnessed closed under their own five universes.
#   YM          — reaches idempotent_terminal under triple_criticality but is
#                 T_CEILING-blocked at Ç (on=4 > ceiling 3): one bump short.
#   RH, NS, PNP — fail at the GATE layer under all 29 universes; no existing
#                 universe reaches idempotent_terminal, with or without T_CEILING.
#
# These charters attack exactly those blockers. RH/NS/PNP need a NEW,
# independently-motivated gate-universe (not a tuple tailored to fit); YM needs a
# motivated reason to raise the Ç ceiling; d=12 needs the exact algebraic fiducial.
# Context = manuscript + UniverseRulesets (ruleset patterns) + the canonical
# tuples (their real primitive profiles) + the current witness module.

name: furnace
out_dir: digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: inf
  temperature: 1.0
  thinking: false
  context: /home/mrnob0dy666/imsgct/dumpdir_furnace

entities:
  # ── RH / NS / PNP — design a NEW motivated gate-universe that closes each ──
  - "the Riemann Hypothesis tuple that fails at the gate layer under all twenty-nine existing universes becomes a new independently-motivated gate-universe whose gate specification is justified by RH's own primitive profile and carries it to idempotent_terminal while staying T_CEILING-consistent"
  - "the Navier-Stokes tuple that fails at the gate layer under all twenty-nine existing universes becomes a new independently-motivated gate-universe whose gate specification is justified by the blow-up/regularity dichotomy and carries it to idempotent_terminal while staying T_CEILING-consistent"
  - "the P-versus-NP tuple that fails at the gate layer under all twenty-nine existing universes becomes a new independently-motivated gate-universe whose gate specification separates the verification arm from the search arm and carries it to idempotent_terminal while staying T_CEILING-consistent"

  # ── YM — a MOTIVATED (non-tailored) reason to raise the Ç ceiling ─────────
  - "the Yang-Mills one-bump-short block, where kinetics ordinal on (4) exceeds the T_CEILING egg (3) ceiling, becomes a motivated non-tailored raise of the Ç anchor grounded in the mass-gap's slow-mode spectrum under which Yang-Mills becomes T_CEILING-consistent"

  # ── d=12 SIC — the exact algebraic fiducial (the analytic obligation) ─────
  - "the d=12 SIC fiducial known only as a complex128 float certificate at frame potential 143/169 becomes an exact algebraic fiducial over its Zauner number field with machine-checkable equiangularity that discharges crystal_forces_d12_sic"
