Design calls are uncapped

max_tokens=None on the design query, and the lane named in the batch yaml rather
than left empty. An empty lane fell through to a local model returning malformed
JSON and then to a provider with no credit, which is how a batch reports "all
providers failed" while a working key sits in the environment.
