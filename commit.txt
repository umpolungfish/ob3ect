run.sh for the Erdos objects batch

One command, and the key stays in the environment: a credential written into the
tree is published the moment the tree is. It runs the batch from its yaml and
then re-checks every object against the corrected gates, since a grounded object
can still read invalid from a stale check.
