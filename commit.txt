d12 existence (v4) batch + bank the v3/fold run trail

Adds d12_existence_batch.yaml, the v4 remake of the d=12 axiom-drop route after
cont.13/cont.14 landed in p4rakernel: both IsSICPOVM halves (norm 897e3d5,
equiangularity 3162fa6) are now green Lean native_decide theorems on the pinned
data, so v3's tower-for-the-143-identities premise is superseded. The remaining
obligation is the EXISTENCE statement: construct the twelve coordinates as exact
flat tower elements (five magnitude covers over K16, deg 512, joined with the
zeta12 / D4-32 / order-32-64 phase fields), prove the constructed overlaps land
on the pinned minimal polynomials (tower involution = conjugation), and transport
along the branch-selecting ring homomorphism to C. Seven entities: re-entry,
tower, coordinates, crux tie, cost-center fork (orbit staging vs per-overlap
re-representation), capstone, gate. Context file banked at
~/imsgct/d12_sic_build/d12_existence_ctx.md.

Also banks the uncommitted run trail from the sanctioned earlier batches
(build2b, build3, discharge_v3, phase_fold and their digital/ outputs, all
Frobenius PASS in session) plus the auto_config.yaml sample.
