Remake phase-section ob3ects for the DIRECT-BUILD frame — 2026-07-03 (cont.2)

New batch d12_phase_direct_batch.yaml + 5 generated ob3ects (digital/), all valid, 12
opcodes each, ZERO fallbacks. This REMAKES the d12_phase_section batch after the
phase-degree-26 gate (FINDINGS cont.2) invalidated its polar-split premise.

What the gate changed:
 - "phase degree 26" is a GHOST (shallow point on the pre-floor creep). Genuine gated
   degrees: z1 phase = 32 (floors, 32|288, IN K288); z5 phase = 64 (floors at 64, 64
   does NOT divide 288, OUT of K288); z0 phase = 1 (z0 real).
 - So the POLAR magnitude x phase split is NOT field-closed. The two prior ob3ects
   #1 (magnitude skeleton) and #2 (phase-in-the-tower) rest on a false premise and are
   SUPERSEDED by this batch. Old batch stays in history; do not build on #1/#2.
 - z1 coordinate creeps past deg 72 with no floor => direct algdep precision-walled at
   1500 digits; unknown yet whether coordinates divide 288.

The five remade entities (EVALT/EVALF polarity commits verified semantically correct):
1. direct-coord    — build z_k as one field element, not magnitude x phase (EVALT=
                     degree-divides-288 pass / EVALF=fail; BOTH="magnitude in-field but
                     phase out-field").
2. field-determine — re-refine fiducial past 1500 digits, floor-test each coordinate to
                     settle if it divides 288 (EVALT=plateau / EVALF=monotonic creep).
3. square-class    — the modulus square-class in K288 is the invariant behind the z1-in
                     / z5-out asymmetry (EVALT=in-field / EVALF=field-leaving).
4. crux-fusion     — z1/z5 via ray-class fusion group [6,6,2,2] S/T character sum, since
                     algdep is walled AND polar is broken (EVALT=deg-32 floor / EVALF=
                     deg-26 ghost).
5. floor-gate      — the deep-vanish floor test as standing admission gate; its BOTH
                     lane names the mechanism: "reducible multiple, degree genuine but
                     non-minimal" (why 26/48/68/139 fooled the weaker checks).

Context: refreshed d12_phase_ctx with the corrected FINDINGS + phase_floor_test.txt +
coord_floor_test.txt + K16_exact_moduli.txt. Generated via google/gemini-3-flash-
preview (openrouter), retries 2.

--- prior (superseded premise) ---
Stage d12 phase-section ob3ects — 2026-07-03
6 ob3ects for the polar magnitude/phase frame; #1 magnitude + #2 phase-in-tower now
superseded (polar split not field-closed). #3 crux, #4 reassembly, #5 reproduction,
#6 143-overlaps remain broadly valid, reframed for direct build.
