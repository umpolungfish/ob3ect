A pass because nothing was at risk is marked as one.

banked_count_check flagged only a clear that loses weight with nothing behind
it, so a word whose clears never fire read identically to a word that banked
correctly. Those are different facts and the difference is the whole content of
the check.

Three verdicts now instead of two. Weight cleared with nothing banked, which is
the failure. Weight that survived a live clear by being held in a frame, which
is the technique working. And nothing ever at risk, reported with the deposits
made and the steps left inert after a fixation, since that is usually why: a
word that commits early leaves its whole second region unexecuted, and its
reversal never fires.

ob3ect marks the vacuous case ○ against ✓ for an earned pass, and the kernel
says VACUOUS rather than OK.
