The Frobenius verdict is a B4 value, not a bool

SplitFuseReport.validate refused anything but PASS/FAIL, so an ob3ect reporting
an honest B or N was marked invalid. auto.py had already stopped retrying to
force a classical PASS — that demand is what pushed the designer to reason toward
domain invertibility instead of describing the fork — and this validator was the
last place still asking for the bool. T, F, B and N are accepted now, with F and
N carrying a reason.

Also adds nests/straus: ten imscriptions as stepping stones toward the rung
bound, each an exact question with no candidate named.
