lattice_cycler and weight_flow, the CLIs the shared module came from.

cycle a word around its ROTAT orbit and read the map from cut to landing
register; account for the weight the union throws away. The rules now live in
imscribing_grammar/scripts/lattice_flow.py so the agents and MoDoT reach the
same implementation; these remain as the command-line face of it.
