bstate-filtration batch: stage the natively-B campaign — the B-state as canonical no-information state, collapse as forced filtration.

Six entities for auto.py, the formal half of what ig-pulse measured. The observatory showed the world through the d=12 SIC frame never once resolving to a definite state over 2394 snapshots — near-maximal entropy, uniform coverage of all 144 directions, zero near-pure readings. This batch stages the theorems that make "all binary collapse is forced filtration" provable rather than rhetorical: (A) the native-B reading with the glut/gap discrimination and its honest-scope caveat carried inside the entity; (B) the flat-readout plank, Tr(E_pq rho_B) = 1/144 exactly for all 144 displacements, riding the frozen fiducial; (C) the no-unitary-collapse plank, purity invariance forever separating B from definiteness so only lossy POVM selection reaches a pure state; (D) the coreflector bridge, ClassicalRestriction.lean's B-to-F collapse and the rank-one selection as the same one-way move at the logic and Hilbert levels; (E) the decisive robustness pass, recomputing the convention-independent signatures under both reconstruction weightings so natively-B either stands on data alone or the ledger says it leaned on its own convention; (F) the capstone gate, SIC_D12_BState in p4rakernel, native_decide standard, no new axioms, the two registers never borrowing each other's authority.

Fresh Grammar verdicts banked verbatim in ctx/bstate_filtration/ alongside frozen sources (ClassicalRestriction.lean, SIC_D12_Embedding.lean, density_matrix.py, sic_povm_convergence.md): the SIC frame is B's mirror at distance 4.5, two moves, symmetric; quantum_measurement_collapse sits eleven forced moves away at asymmetric distance (cheaper to fall in than climb out — the metric echo of the failed reverse adjunction); and tensor(void_b_state, quantum_measurement_collapse) is DENIED on the same six primitives as the vessel campaign's cargo denial. Definiteness is not adjoinable to B. It is only reachable by filtration, and the filtration forgets.
