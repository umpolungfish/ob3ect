Ban bullets and dashed lists in editorial pipeline and auto.py — 2026-06-29

editorial_pipeline_ob3ect.py:
  _STYLE_PROHIBITIONS: added "No bullet points, no dashed lists, no numbered
    lists — write as prose only."
  _critique_prompt: added bullet/dashed/numbered list violations to the
    explicit flag list.

auto.py:
  HARD PROHIBITIONS Structure block: added bullet points, dashed lists,
    numbered lists (any list format banned — prose only).
  _ZOOM_SYSTEM: same addition to the abbreviated prohibition tail.

---

editorial_pipeline: add _STYLE_PROHIBITIONS to all prompt functions — 2026-06-29

The auto.py prohibition only covered ob3ect design generation. The editorial
pipeline (editorial_pipeline_ob3ect.py) has its own prompt chain and was
generating em-dashes and banned words freely.

_STYLE_PROHIBITIONS: shared constant banning em-dashes (in any form), banned
  words (delve, tapestry, leverage, seamless, multifaceted, pivotal, etc.),
  banned openers (Furthermore, Moreover, Certainly, etc.), banned phrases
  ("it is important to note", "in essence", etc.), trailing restatements,
  false-balance hedging.

Injected into _edit_prompt, _rewrite_prompt, and _critique_prompt.
_critique_prompt now explicitly flags prohibition violations in its checklist.

auto.py em-dash prohibition tightened: "no em-dash filler padding" → "no
  em-dashes at all (not as connectors, not as asides, not anywhere)".

---

auto.py: catalog access + AI writing pitfall prohibition — 2026-06-29

_search_catalog(description, n=8): keyword overlap search against all 4686
  catalog entries (name×3 + description weight); injects top-N as a compact
  <catalog-reference> block into every _build_prompt call automatically.
  Returns empty string (no injection) when no tokens match, so unrelated
  domains don't get noise. Catalog loaded once and cached in _catalog_cache.

_build_prompt: adds catalog_section before context_block so the model sees
  nearest IG structural precedents before designing the new ob3ect.

_SYSTEM_PROMPT: added WRITING QUALITY — HARD PROHIBITIONS section.
  Banned words: delve, tapestry, leverage (verb), utilize, seamless,
    multifaceted, pivotal, cutting-edge, synergy, holistic, paradigm (filler),
    testament, underpinnings, realm, landscape (metaphor), robust (filler),
    revolutionary, groundbreaking, transformative, nuanced (filler), etc.
  Banned phrases: "it is important to note", "in essence", "moreover" (opener),
    "furthermore" (opener), "certainly!", false-balance hedging, trailing
    restatement summaries, em-dash filler padding.

_ZOOM_SYSTEM: same abbreviated prohibition added.

---

sic_povm_functor + sic_povm_parity_gate vault stubs upgraded — 2026-06-27

digital/.vault/sic_povm_functor/sic_povm_functor_ob3ect.py — Crystal->H^12 functor:
  Verifies all 11 dual-lattice identities: Crystal product, Lattice I (3+5+4=12),
  Lattice II (3x4=12), dual-lattice agreement, Shavian count (49=7^2), d_DP=7,
  composite d=84, orbit 84^2=7056=49x144, coverage 17.28M/144=120000.
  STATUS: PASS tier O-infty. ΔS=0 (SIC informational completeness; 120000x overcomplete).

digital/.vault/sic_povm_parity_gate/sic_povm_parity_gate_ob3ect.py — Phi gate:
  Verifies T<->P duality: 5x4=4x5=20 (slot symmetry), (5,4)<->(4,5) transposition,
  Phi involution, d=7 from {D,P} (Phi lives in P-family; D+P=3+4=7 < D+T=3+5=8),
  Shavian count 9+20+20=49=7^2 from parity structure, orbit 84^2=7056=49x144.
  Reports absorbing element: Criticality.woe (idx 0).
  STATUS: PASS tier O-infty. ΔS=0 (T=P slot count, no information asymmetry).

────────────────────────────────────────────────────────────────────────────────

sic_povm vault ob3ect: d=12 Crystal-forced SIC-POVM verification — 2026-06-27

digital/.vault/sic_povm/sic_povm_ob3ect.py — replaced stub with exact d=12 verification:
  - Loads cached fiducial from ig-pulse/data/sic_fiducial_d12.npy (3ms; recomputes if absent)
  - Builds all 144 Weyl-Heisenberg operators D(p,q) = e^{i pi pq/d} X^p Z^q
  - FSPLIT/FFUSE: verifies all 143 non-identity WH-orbit overlaps = 1/13 (Frobenius PASS, delta=1.3e-08)
  - Frame potential F = 0.846154 = (d^2-1)/(d+1)^2 exact (OK)
  - Reports Zauner symmetry as info only (non-symmetric fiducials are valid)
  - STATUS: PASS tier O-infty
  - ΔS = 0 (SIC informational completeness, lossless reconstruction)

digital/.vault/sic_povm/sic_povm_ob3ect.json — updated:
  - phase_0 boundary_condition: Crystal 3^3 x 4^5 x 5^4 = 17,280,000, both SIC lattices
  - phase_2 split/fuse: WH displacement / frame potential (replacing Dirichlet/Stark stubs)
  - phase_2 test_instance: exact fiducial amplitudes [0.1766, 0.1396, ..., 0.3156]
  - phase_6 post_cycle_state: d=12 confirmed tier O-infty
  - phase_6 crystal_derivation: Lattice I (3+5+4=12), Lattice II (3x4=12), 84^2=7056 composite
  - phase_6 independent_confirmation: DeepSeek R1 VAE-Vita result

Bootstrap behavior (cr3 sic_povm) unchanged at B-state: correct, as the Zauner conjecture
for general d remains open/paraconsistent. The Python standalone demonstrates the d=12 resolution.

────────────────────────────────────────────────────────────────────────────────
Previous entries
────────────────────────────────────────────────────────────────────────────────
ob3ect/auto.py: add ZoomChain — Γ-axis traversal of nested IMASM levels — 2026-06-27

New zoom chain capability. A ZoomChain is a sequence of N full Ob3ectArtifacts spanning
from a fine-grained seed concept (Γ=0) to a coarse-grained target concept (Γ=N-1), with
CLINK morphisms describing how each level composes into the next.

New additions:

  _discover_zoom_levels(seed, target, n_levels, temperature)
    Single LLM call returning the N-level Γ hierarchy and N-1 CLINK morphism descriptions.
