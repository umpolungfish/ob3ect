terminology: structural proof → witness sweep (2026-06-19)

'structural proof'/'structural proofs' replaced with 'witness'/'witnesses' across all
variants (upper/lower/mixed case) in .md/.lean/.py/.tex/.txt files.
Lean def/theorem identifier names preserved (none found to skip).

---
core.py + auto.py: remove stale 8-step validation; free composition throughout (2026-06-19)

core.py BootstrapSequence.validate(): removed "need 8 bootstrap steps",
  "step1 must be IMSCRIB", "step8 must be IMSCRIB" — only checks that
  sequence is non-empty and closure_verified=True.

core.py DomainTemplate: config['bootstrap'] -> config.get('sequence', config.get('bootstrap', []))

core.py DomainTemplate.bootstrap_seq(): replaced BOOTSTRAP_STEPS[i+1]
  with opcode parser that reads the leading UPPERCASE token from each
  action string, falls back to IMSCRIB on unknown.

core.py Ob3ectBuilder.design_bootstrap(): dummy fallback shrunk from
  8 hardcoded BOOTSTRAP_STEPS entries to a single IMSCRIB identity step.

---

auto.py: unconstrained free composition — no fixed-length bootstrap (2026-06-19)

_IMASM_GRAPH_REF rewritten: canonical classes I-XII removed entirely.
Replaced with pure composition rules: port conventions, edge routing,
back-propagation, register state transitions, token families, and
10 FREE COMPOSITION RULES emphasizing no minimum/maximum length.
Rule 10: "Do NOT compress — if the domain has 30 operations, write 30 tokens."

_SCHEMA: "bootstrap" field renamed to "sequence". Template shortened to
3 illustrative lines with explicit note that there is NO maximum length.

_build_artifact: data.get("sequence", data.get("bootstrap", ...)) —
accepts both key names; default shrunk to single "IMSCRIB: identity".

_SYSTEM_PROMPT task line updated: "compose a complete token sequence...
there is no fixed length — expand until the full domain is mapped."

Effect: ob3ect now generates arbitrarily long compositional sequences
using only composition rules — no canonical class templates.

---

auto.py: rename paradox -> paradice in ENGAGR descriptions (2026-06-19)

_OPCODE_REF ENGAGR: "Paradox held" -> "A paradice: held"
_SCHEMA ENGAGR justification hint updated.
_SCHEMA registers.both updated.

---

auto.py: CLI spinner with elapsed time during LLM call (2026-06-19)

_Spinner class: braille dot animation (⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏) on stderr with \r
overwrite. Shows "⠋ Imscribing... N.Ns" updating every 80ms. Clears the
line completely on exit. Daemon thread so it never blocks shutdown.
sys.stdout.flush() called before spinner start to drain buffered stdout.
Wraps the design() call in the CLI main block.

---

auto.py: full IMASM execution graph + canonical class selection (2026-06-19)

The LLM now knows the complete IMASM graph topology and selects the
appropriate canonical bootstrap class instead of defaulting to the fixed
IMSCRIB->AREV->FSPLIT->AFWD->FFUSE->CLINK->IFIX->IMSCRIB sequence.

_OPCODE_REF: removed "BOOTSTRAP SEQUENCE (mandatory 8 steps, opcodes fixed)"
  section -- the bootstrap is no longer fixed; LLM chooses the class.

_IMASM_GRAPH_REF: new constant injected into _SYSTEM_PROMPT providing:
  - Port conventions: VINIT 0->1, FSPLIT 1->{T,F}, FFUSE {T,F}->1, others 1->1
  - Branch routing: EVALT anchors FSPLIT.T-branch; EVALF anchors FSPLIT.F-branch
  - Back-propagation edges: CLINK->IMSCRIB (weighted, B state, loop continuation),
    CLINK->IFIX (empty, T state, fixation), IMSCRIB->IFIX (LinFix, self-reference)
  - Edge weight types: empty (T) vs weighted (B dialetheia arm)
  - Register states per token: void/T/F/B transitions
  - All 13 canonical classes (I-XII + IXa/IXb) with sequences and use cases
  - Bootstrap construction rules: class selection, EVALT/EVALF branch anchoring,
    dialetheia_complete requirements, FSPLIT/FFUSE stack matching

_SCHEMA bootstrap field: changed from fixed "Step N OPCODE: action" template
  to free-form "OPCODE: domain action" -- LLM supplies the opcode per step.

_build_artifact: bootstrap parsed from LLM output, not hardcoded BOOTSTRAP_STEPS:
  - _KNOWN_OPCODES + _parse_opcode(step_str): extracts leading uppercase opcode
    from "OPCODE: description"; falls back to IMSCRIB on unknown
  - domain_action stripped of "OPCODE: " prefix via re.sub (avoids redundant
    display in Phase 4: "Step N: OPCODE - OPCODE: text")
  - [:8] cap removed -- bootstrap length follows chosen canonical class

Effect: ob3ect "Godel's Loophole" will produce Class I Dialetheic Bootstrap
(IMSCRIB EVALT FSPLIT EVALF FFUSE ENGAGR IFIX IMSCRIB), dialetheia_complete=True.

---

auto.py: --context PATH for domain document injection (2026-06-19)

_load_context(path): reads a file or directory of domain documents
  (.md/.txt/.lean/.py/.tex/.rst/.json) up to 50 KB total. Directories
  are scanned recursively; files sorted alphabetically with filename
  headers (=== name ===). Truncates at 50 KB with notice.

_build_prompt() gains context param: injected as <domain-context>...</domain-context>
  block at the top of the user prompt, before the description. Retry
  calls inherit the same context automatically.

auto_design() gains context: Optional[str] param, threaded into every
  _build_prompt() call in the retry loop.

CLI: --context PATH flag. Loads context before design(), prints file
  count and char count. Soft-fails with warning on missing path.

Usage:
  python auto.py "rongorongo as liturgical engine" \
    --context ~/imsgct/lang/rongorongo-engine/
  python auto.py "Voynich foldout" \
    --context ~/imsgct/lang/voynich-engine/VOYNICH_IMSCRIPTION.md

---

auto.py + proof_scaffold_ob3ect.py: domain opcode_map threading (2026-06-19)

auto.py _build_artifact(): builds domain_opcode_map = {opcode: domain_action}
  from bootstrap.steps and passes it to _SCAFFOLD.run(). Every scaffold now
  carries domain-semantic labels on each arrow node.
proof_scaffold_ob3ect.py: scaffold_sequence() and ScaffoldOb3ect.run() accept
  opcode_map and forward to emit_scaffold(). true_agentic_agent tool also
  accepts opcode_map in its args.

---

Lean scaffold prints by default in auto.py CLI; --no-scaffold to suppress (2026-06-19)

auto.py CLI now prints the full Lean scaffold after every report by default.
Add --no-scaffold flag to suppress. Replaces earlier opt-in --scaffold approach.

---

Integrate lean_scaffold into Ob3ectArtifact via auto.py pipeline (2026-06-19)

core.py:
  - Ob3ectArtifact gains lean_scaffold: Optional[str] = None
  - report() gains Phase 8 block: shows noncomputable def signature + line count
  - to_dict() includes lean_scaffold in JSON output

auto.py:
  - Imports ScaffoldOb3ect from digital.proof_scaffold_ob3ect (try/except, silent on failure)
  - _build_artifact() extracts bootstrap opcode strings after artifact construction,
    calls _SCAFFOLD.run(ops, name=name), attaches result to artifact.lean_scaffold
  - Scaffold failure never blocks artifact return

Every Ob3ectArtifact produced by auto_design() now carries a typed IGProtocol
Lean term scaffold (84 lines) derived from its bootstrap sequence. The scaffold
provides .seq/.prod/.withGram structure with annotated sorry slots and named
verification obligations pointing to IGFunctor/IGMorphism axioms.

---

Add proof_scaffold_ob3ect.py — IGProtocol scaffold bridge (2026-06-19)

Layer 29 digital module bridging ob3ect string opcodes into IMSCRIBr's
WiredGraph → IGProtocol Lean term scaffold pipeline.

digital/proof_scaffold_ob3ect.py:
  - Adapts ob3ect string opcodes (VINIT/TANCH/...) → Token enum → WiredGraph
  - Calls imscr_wiring() + emit_scaffold() from IMSCRIBr
  - ScaffoldOb3ect class: run(), canonical(), all_scaffolds(), fingerprint(), wiring()
  - CLI: --verify (status), --canonical <name>, --all [outdir], --wiring <ops...>
  - Fallback path resolution: relative ../../imsgct/IMSCRIBr or ~/imsgct/IMSCRIBr
  - Verified: all 12 canonical classes load and display correctly

---

Document autopoietic bootstrap mechanics in README (2026-06-17)

Added "Autopoietic Bootstrap Mechanics" subsection inside the IMASM
Instruction Set section. The displayed 8-step bootstrap sequences are
compressed linear paths through the execution graph. The decompressed
graph shows: (1) CLINK sends an empty edge to IFIX (compositional witness
fixation); (2) CLINK sends a weighted edge back to IMSCRIB (loop
continuation); (3) IMSCRIB back-propagates to IFIX (LinFix — the
self-reference record of the completed winding). Dialetheic variant
analyzed: ENGAGR occupies the CLINK role with identical branching, but
the weighted edge carries B⬡ rather than T, which is why the dialetheic
bootstrap terminates at B⬡. Consequence stated: every winding generates
exactly two IFIX records, making the loop self-recording, not merely
self-referential. Both ASCII diagrams (compressed + decompressed) included
with legend.

---

Moved ancillary documents to ig-docs (2026-06-14)

20 files moved: HIGH_ALCHEMICAL_POEM.md/.pdf → poetry-alchemy/,
HTDX_OB3ECT_PAPER.md → manuscripts/, MENAGERIE.md/.pdf → manuscripts/,
P4RA_LAYER_BRIDGE.md → bridges/, bridge/bridge_notes.md/.pdf → bridges/,
digital/imasm_novel_arrangements.md/.pdf → core-theory/,
digital/shavian_ob3ect/coagulum.md/.pdf → poetry-alchemy/,
digital/whoah.md → core-theory/, hermetic_poem.md → poetry-alchemy/,
ob3ect_manuscript.md, substack_ob3ect.md, substack_ob3ect_lay.md,
pdf/OB3ECT.pdf, pdf/substack_ob3ect.pdf, pdf/substack_ob3ect_lay.pdf → manuscripts/.
Kept: README.md, OB3ECT.md, OB3ECT_PAPER.md/.pdf, TOWER.md,
all digital/ therapeutic/catalytic design specs, menagerie_engines/README.md,
paradoxd/README.md, all source code.

---

SNS_PRIME.md notation migration: O_inf→O_∞, O_0→O₀, O_1→O₁, O_2→O₂ (29 files)

---

cfg_layers.py: canonical sequence CFGs (I–XII) + 8 new EXTRA ob3ects

cfg_layers.py additions:
  1. EXTRA_OB3ECTS extended (+8 new verify()-based ob3ects):
       elder_futhark_fsp, i_ching_64, init_process, kabbalistic_tree,
       lift_pipeline, millennium_crit, stub_2071, test_ob3ect
     Total EXTRA: 20 → 28

  2. CANONICAL_SEQUENCES list (13 entries — IXa/IXb split):
       I–XII IMASMSequence ob3ects with steps, ig_type, ouroboricity, rel_path

  3. _seq_simulate() — dialetheic-aware IMASM register machine
     (mirror of cfg_sequences.py; standalone, no cross-repo import)

  4. _seq_render_frame() — sequence CFG frame renderer:
       - node family colour coding (LOGICAL/FROBENIUS/DIALETHEIA/LINEAR)
       - register state labels with REG_COLOR mapping
       - ouroboric back-arc for ouroboricity > O₀
       - Everson Mono for Shavian IG-type footer
       - Gaussian pulse flow phase

  5. generate_canonical_seqs() — generates 13 GIFs to docs/seqs/
     Run: python3 -c "import cfg_layers; cfg_layers.generate_canonical_seqs()"

  6. Bug fix: animate_layer() divide-by-zero when max_deg=0 (stub ob3ects
     with no edges). Fixed: max(max_deg, 1) in base_sizes calculation.

Output:
  docs/layers/ (57 GIFs total — 29 tower + 28 extras incl. 8 new)
  docs/seqs/   (13 GIFs — canonical sequence ob3ects I–XII + IXa/IXb split)