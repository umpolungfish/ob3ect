Stage d12 phase-section ob3ects — 2026-07-03

New batch d12_phase_section.yaml + 6 generated ob3ects (digital/), all valid, 12
opcodes each, ZERO API-error fallbacks. Staged AFTER the moduli problem closed: the
12 squared moduli are now exact in the degree-16 real field K16 and the SIC
normalization sum|z_k|^2=1 is a proven exact identity, so this batch scopes the ONLY
remaining unknowns — the phases and full-coordinate reassembly.

The six entities:
1. magnitude    — |z_k| = positive sqrt of the exact K16 modulus; real skeleton fixed.
2. phase-unit   — u_k = z_k/|z_k| confined to the layers K16 does not reach (i-piece
                  + the two cubic pieces of the FSPLIT tower).
3. crux z1/z5   — recovered as a ray-class fusion-group [6,6,2,2] orbit via modular
                  S/T data (SIC-Galois-tensor == WZW == affine-Kac-Moody isomorph),
                  not brute algebraic dependence.
4. reassembly   — z_k = |z_k|*u_k as one length-288 flat rational vector for the
                  SIC_D12_NumberField native_decide engine.
5. reproduction — the 12 exact coords certified against the 1500-digit witness + gate.
6. 143-overlaps — the WH overlap identities closed by native_decide, discharging
                  crystal_forces_d12_sic axiom -> theorem.

Context built from today's resolved state: FINDINGS.md, K16_exact_moduli.txt,
FSPLIT_tower_6pieces.txt, the flat Lean engine. Generated via
google/gemini-3-flash-preview (openrouter), retries 2.

CAUTION carried into the section: the phase-unit ob3ect's BOTH-lane inherited the old
"phase degree 26" claim from context; that number is one FINDINGS flags as a possible
low-precision artifact — gate-verify it before trusting, do not build on it directly.
