The tools carry both rules, not one.

Banking and surplus are independent, proved in
p4ramill Imscribing/IMASM/BankedWeight.lean, and every tool reported only the
first. Whether ANYTHING is banked is decided by a frame being open when the
clear fires. Whether the SURPLUS is kept is decided by where the splits fall
between deposits of the same value: deposits inside one region accumulate, the
fold between regions takes a maximum, so a value deposited in two sibling
regions comes back as one.

banked_count_check reports the surplus alongside the verdict, with the note that
a split between two deposits of the same value puts them in sibling regions.
ob3ect prints it under Phase 9b. The kernel prints the repeat-deposit warning
after an OK. The design prompt states both rules and says plainly that neither
implies the other. The technique document is split into Rule 1 and Rule 2, with
the formula that matches the ledger and the two earlier attempts named as the
special cases they are.
