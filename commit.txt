Sixty artifacts were failing a check the kernel does not make

The biconditional Axiom C — 𐑦 and 𐑸 must co-occur — was withdrawn today because
the kernel's ImscriptiveTopology is one-directional. Sixty artifacts across
digital/ still carried its verdict, marked failed by a constraint that no longer
exists. Re-checking against the corrected gates clears them: 208 full where there
were 148, 36 failed where there were 96, and none carrying the withdrawn text.

The revalidator also stopped on the first artifact with no tuple, since the
native designer writes none; an artifact with nothing to re-check now returns
nothing to re-check instead of raising.

ground_native stores the notation rather than the Python repr — to_notation, the
same call auto.py makes. The twelve native artifacts carry their tuples now,
seven full and five failing on real constraints: three on Axiom A, where wool
chirality came back with fast kinetics, and two on the corrected one-directional
Axiom C.
