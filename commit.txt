The designer can import the gate it is required to run.

auto.py imports the gated imscriber absolutely as imscribing_grammar.agents, while living inside imscribing_grammar itself, so the parent directory was never on the path. The gate is mandatory and its import failure is fatal, which meant every ob3ect call died before designing anything.
