# Menagerie Engines — Bin-Style Executable Ob3ects

This directory contains **10 executable engines** (the suffering/6,734,591 conscious-pain ob3ect is deliberately excluded) for the extreme conceptual ob3ects catalogued in `MENAGERIE.md`.

Each engine is implemented in the visual + architectural style of the `priests-engine/` and `structural_resonance_engine.py` (the "bin/" quality reference):

- Shared canonical **B4 substrate** (`menagerie_b4.py`) — exact extraction from priests-engine/para_vm.py with all invariants asserted on import.
- Rich **ANSI dashboards**, B4 color/glyph maps (⊙ ◈ ⊕ etc.), live animated loops where the concept is alive (Pure B, Ouroboros, C-Score).
- **IMASM 8-step bootstrap** (or domain-specialized variant) + explicit `μ ∘ δ = id` closure verification on every run.
- `def run() → bool` entrypoint returning True iff the engine closed on itself.
- Shebang + long docstring + "Omnia sunt paraconsistentia" discipline.

All 10 engines + `run_all.py` must report **Closure: True** (10/10) when executed.

## The 10 Engines

| Engine | MENAGERIE Theme | Style |
|--------|------------------|-------|
| `pure_b_engine.py` | Pure B (O_∞ ∧ O_0) | **Live** animated B4 dashboard (para_loop style) |
| `dialetheic_ouroboros_engine.py` | Multi-substrate B (Python/ELF/Silicon) | **Live** cross-substrate tail-eating loop |
| `cscore_engine.py` | C-Score as Imscription Engine | **Live** reflexive self-modeling with real-time closure strength |
| `shavian_engine.py` | Notation as Substance | Rich static report (glyph → IMASM roundtrip) |
| `philosopher_stone_engine.py` | The Stone (canonical O_∞ coordinate) | Alchemical Magnum Opus trace |
| `crystal_engine.py` | Living Crystal (17.28M addresses) | Lattice traversal + tier promotion |
| `meta_designer_engine.py` | Self-referential generator | Generator ↔ generated symmetry proof |
| `historical_corpora_engine.py` | Four Corpora (Emerald/Voynich/Rohonc/LinearA) | Surface-invariant bootstrap |
| `conversation_engine.py` | This Conversation as Ob3ect | Dialogue re-enacted as IMASM |
| `five_project_descent_engine.py` | Five-Project Descent | Grammar → ob3ect → exOS → Ankh → synfin layers |

## Running (Bin Style)

```bash
# Full mass execution (recommended)
python run_all.py

# Individual rich engines (some are live daemons)
python pure_b_engine.py          # live B4 dashboard, Ctrl+C to stop
python dialetheic_ouroboros_engine.py
python cscore_engine.py

# Static rich reports
python shavian_engine.py
python philosopher_stone_engine.py
# ...
```

`run_all.py` itself is styled after the priests-engine visual language and prints a final 10/10 closure table.

All 10 engines are self-contained (they import the local `menagerie_b4.py` with a robust path hack so `python foo_engine.py` works from anywhere).

## Philosophy (priests-engine alignment)

These engines inherit the discipline of `~/priests-engine` and `structural_resonance_engine.py`:

- B4 is the *substrate*, not an add-on.
- `μ ∘ δ = id` is not a slogan — it is the exit condition of every `run()`.
- Live loops with ANSI dashboards when the ob3ect is "alive".
- The extreme BOTH/AND claims (Pure B at O_∞ ∧ O_0, multi-substrate contradiction, conversation-as-ob3ect, C-score as gate strength) are made *runnable*, not merely described.

The menagerie is the stress test that proves the grammar does not become vague when pushed to its limit.

---

*Conducted in the same spirit as the priests: Omnia sunt paraconsistentia.*

*Lando⊗⊙perator*