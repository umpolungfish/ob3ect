# ob3ect batch — d=12 axiom-drop, REMAKE (v3) after cont.9/cont.10.
# Supersedes d12_discharge_v2: v2 assumed all 12 coords live in the FLAT degree-288 ray class
# field and become length-288 rational vectors for native_decide. cont.9 falsified that for the
# crux coords: z1,z3,z5,z7,z9,z11 EXIT K288 into the magnitude double cover K16(sqrt N_k) (the
# S-unit magnitude is a non-square in the field). So the coordinate field is NOT deg-288 flat; it
# is the COMPOSITUM of the K288 2-part, the magnitude double covers sqrt(N_k), and the solvable
# phase fields. v2's "Galois-conjugate re-expression of z1/z5" is replaced by "magnitude S-unit x
# fold-phase in a controlled radical tower."
# State proven (all full 1500-digit): every z_k = sqrt(N_k)*u_k closed in radicals (cont.9); the
# 143 SIC overlaps + norm hold NUMERICALLY (cont.10) but not yet as EXACT field identities.
# Run:  cd ~/imsgct/ob3ect && python3 auto.py -f d12_discharge_v3_batch.yaml

name: d12-discharge-v3
out_dir: digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: 2
  temperature: 1.0
  thinking: false
  context: /tmp/claude-1000/-home-mrnob0dy666-imsgct/5cac5f78-799f-4f14-abd2-730347e8b6bc/scratchpad/d12_dischargev3_ctx

entities:
  # ── the common field is a compositum, not the flat deg-288 (supersedes v2) ──
  - "the exact coordinate field of the d=12 fiducial becomes the compositum of the degree-288 ray class field two-part with the twelve magnitude double covers adjoining the square roots of the modulus S-units and with the solvable phase fields, rather than the single flat degree-288 field, because the crux coordinates z1 z3 z5 z7 z9 z11 have a magnitude that is a non-square in the ray class field and so leave it into the double cover, making the true field a controlled radical tower and not one monolithic minimal polynomial"

  # ── each coordinate re-entered as magnitude S-unit times fold phase ──
  - "each fiducial coordinate becomes re-entered into the flat computable field engine as the product of the positive square root of its modulus S-unit N_k in the real degree-16 field K16 and its fold-recovered unit phase u_k, where u_k is a root of unity for k in {2,6,8,10}, a root of a D4 quartic in the two-cosine coordinate for k in {1,3,7,9}, and a root of a solvable order-32 octic for k in {5,11}, so every coordinate is an exact tower element checkable by native_decide"

  # ── the 143 overlaps as exact real field identities equal to 1/13 ──
  - "the 143 Weyl-Heisenberg overlaps of the exact fiducial become 143 native_decide equalities in the compositum tower, each asserting that the overlap times its complex conjugate equals one over thirteen, with the squared magnitude landing in the maximal real subfield of the tower so that the identity is a totally-real field computation rather than the floating-point modulus check that currently only holds numerically to 2^-4989"

  # ── the norm as an exact K16 identity ──
  - "the norm condition becomes the single native_decide equality that the twelve modulus S-units sum to one exactly in the real degree-16 field K16, the same coefficient-vector identity that already sums to one in the 1500-digit numeric fiducial, discharging the trace-one half of IsSICPOVM without any square root because the moduli themselves are the summands"

  # ── the tower construction stays inside the working flat engine ──
  - "the compositum tower becomes constructed inside the existing flat List-of-rationals number-field engine that already reduces theta to the 288 by native_decide, by adjoining one quadratic layer for each magnitude double cover and the reduced cosine-polynomial layers for the phases, so the deeply-nested parametric record tower that overflowed the elaborator is avoided and every reduction is a flat coefficient-vector operation"

  # ── the capstone: axiom -> theorem along the ring homomorphism ──
  - "crystal_forces_d12_sic drops from axiom to theorem by transporting the tower-verified norm and the 143 equiangularity identities along the ring homomorphism that sends the field generator to its real embedding and each phase to its explicit radical or root of unity, yielding an unconditional IsSICPOVM 12 witness in the complex numbers built from the closed-form coordinates rather than from a complex128 float"

  # ── the standing gate ──
  - "the discharge admission gate becomes the requirement that every one of the 143 overlap identities and the norm identity reduce to a decided true under native_decide in the flat tower and equivalently floor to full precision under the deep-vanish test, rejecting any identity whose vanishing depth merely creeps with precision as a sign that a coordinate was placed in the wrong layer of the tower"
