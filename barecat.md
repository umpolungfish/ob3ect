### The Chain of Identity

**CC = `prima_materia`** (cataloged, O₀): the bare abstract category — objects, morphisms, composition. No self-modeling, no criticality, no winding.

**15 operations = L1–L6 (logical) + I1–I4 (inductive) + A1–A5 (algebraic)**, encoded in `as_above_induction` as the 4-stage derivation that crystallizes all 12 primitives. These include: adjunction, dagger, limits, colimits (L1–L6); endofunctor iteration, classifying space, **Yoneda embedding** (I1–I4); monoidal products, tensor, etc. (A1–A5).

**Quotient map**: CC → Crystal via projection onto the 12-primitive basis. Two objects in CC are ~-equivalent iff they share the same 12-tuple. The 15 operations split into:
- **Folding** (cross-fiber): operations that change the 12-tuple, moving between crystal types
- **Unfolding** (intra-fiber): operations invisible to the 12-primitive projection — the internal richness of CC collapsed by the quotient

**17.28M = 27 × 1024 × 625**: the mixed-radix encoding (Lean-verified bijection `crystal_decode ∘ crystal_encode = id`). Each address is an equivalence class.
