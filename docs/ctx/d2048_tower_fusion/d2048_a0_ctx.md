# d2048-a0-moduli-pin — frozen context

**One plank only:** pin the **a=0 stratum** of the d=2048 F-arm shadow — eleven overlaps
`O_{0,b}` for `b ∈ {1,2,4,8,16,32,64,128,256,512,1024}` — as **exact identities** in a
`ℚ(√4190205)`-based modulus tower, mirroring d=12 `mini_engine_a0.py` + `build_k16_v3`.

**Does NOT close** `SICPOVM_Exists 2048`. Does NOT use scipy. Does NOT conflate T-arm skeleton
with F-arm shadow.

## Ascent placement

| Layer | Catalog | Status |
|-------|---------|--------|
| L1 Ray class field | `sic_hilbert12_connection` | DONE — `field_skeleton_2048.txt` |
| **L2 Ramified double cover** | **`sic_geometric_lift_hypothesis`** | **THIS OB3ECT** |
| L3 2048-dim ring | `belnap_multilattice_sic_povm` | DONE — `transport_b_fiducial` (T-arm) |
| L4 Deep-vanish floor | `universal_imscriptive_grammar` (⊞=𐑙 ideal) | Later |

Tuple anchor: ⟨𐑨𐑸𐑽𐑹𐑐𐑧𐑲𐑵⊙𐑫𐑳𐑴⟩, C=0.736

## Register separation (load-bearing)

| Register | Lean object | Role |
|----------|-------------|------|
| **T-arm** | `transport_b_fiducial` | `ω^hamming(k)/√2048` — proved norm=1, NOT SIC |
| **F-arm** | `fiducial_from_stark_2048` | `embeddings_2048(k)(stark_unit_2048)` — open target |
| Obstruction | `BelnapRing2048.wh_char_two_valued` | F cannot be read from T |

## WH convention (Lean `D_ah`)

```
(Dψ)_k = ω^{b(k−a)} ψ_{k−a},   ω = exp(2πi/2048)
```

**Not** Pauli+τ. **Not** mislabeled `coordinate_witness_1500.txt` (that is d=12).

## The eleven a=0 identities (the obligation)

For `b ∈ {1,2,4,8,16,32,64,128,256,512,1024}`:

```
O_{0,b} = Σ_k ψ̄_k · ω^{bk} · ψ_k
O_{0,b} · Ō_{0,b} = 1/2049
```

When `ψ_k = √N_k · u_k` with `|u_k|=1`, phases enter through the weighted sum; at a=0
the **modulus spectrum Fourier modes** at those eleven frequencies are primary unknowns.

Stratum reps: `orbit_table_2048.txt` lines 139–149 (2-adic labels `v₂(a)=999`).

## Why scipy failed (Stage 3b finding)

`stage3b_a0_floor.json`: Stage 3 witness has `|O_{0,b}|² ≈ 10⁻⁷`, not `1/2049`.
Reported `max_res ≈ 0.000488` meant `|0 − 1/2049|`, not floor convergence.
**Algebraic moduli as S-units must precede any numerical flow.**

## Base field (computed)

```
m_d = (2048−3)(2048+1) = 4190205 = 3·5·409·683  (squarefree)
F_{2048} = ℚ(√4190205)
h = 64,  R ≈ 7.624   (field_skeleton_2048.txt)
```

## d=12 template (mirror exactly)

| d=12 | d=2048 analog |
|------|---------------|
| `mini_engine_a0.py` — 11 overlaps, `ALL PASS` in fractions | `a0_engine_2048.py` |
| `build_k16_v3.txt` + `pin_sunit.gp` | `build_f2048_*.gp` |
| `K16` totally-real moduli field | totally-real subfield over `ℚ(√4190205)` |
| 143 overlaps → 31 Galois orbits | 4194303 → 143 strata → **11 a=0 reps first** |

## Orbit staging (already computed)

- Raw WH pairs: 4,194,303
- 2-adic strata: **143** (mirrors d=12's 143 raw overlaps)
- Symplectic+Zauner coarse orbits: 11 — **all `(0,b)`**

## Deliverables this ob3ect must emit

1. `build_f2048_*.gp` — modulus witnesses over `ℚ(√4190205)`, S-unit pin
2. `a0_engine_2048.py` — fraction re-verify; prints `ALL 11 PASS` or first failure
3. `orbit_a0_2048.txt` — eleven `(0,b)` reps + 2-adic labels
4. `SIC_D2048_A0Stratum_scaffold.lean` — 11 `native_decide` slots; **no theorem claim**
5. `FINDINGS_d2048_a0.md` — pass / fail / infeasible + next stratum

## Admission gate

- Eleven a=0 identities pass in **exact arithmetic**, not float creep
- Every pinned modulus exhibited as **S-unit** of `F_{2048}`
- `deep_vanish_floor` only if residuals `< 10⁻⁴⁰⁰` at 1500 digits on all eleven reps
- Register separation stated explicitly in findings

## What NOT to do

- Close `SICPOVM_Exists 2048` or `ZaunerBridge 11`
- Use `transport_b_fiducial` as shadow fiducial
- `native_decide` all 4,194,303 overlaps
- Seed from mislabeled d=12 coordinates

## Files frozen in this ctx/

Lean: `ZaunerTransportMap.lean`, `Ring2048_Bridge.lean`, `SIC_POVM_Stark.lean`, `BelnapRing2048.lean`

d=12 template: `mini_engine_a0.py`, `build_k16_v3.txt`, `pin_sunit.gp`

d=2048 campaign: `orbit_table_2048.txt`, `field_skeleton_2048.txt`, `stage3b_a0_floor.json`,
`ascent_2048_campaign.json`, `ascent_verification.json`

## Run

```bash
cd ~/imsgct/ob3ect && python3 auto.py -f yamls/d2048_a0_moduli_pin_batch.yaml
```