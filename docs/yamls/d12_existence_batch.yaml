# ob3ect batch - d=12 axiom-drop, REMAKE (v4 "existence") after cont.13/cont.14.
# Supersedes d12_discharge_v3: v3's 143-overlaps-in-tower and norm obligations are DISCHARGED
# (SIC_D12_Norm 897e3d5, SIC_D12_Equiangularity 3162fa6 - both IsSICPOVM halves are green Lean
# native_decide theorems on the pinned data; the overlaps turned out to be SMALL, max degree 32,
# so no compositum was needed for the identities). What remains of crystal_forces_d12_sic is the
# EXISTENCE statement: construct the twelve coordinates as exact Lean tower elements, prove the
# overlaps of the CONSTRUCTED coordinates realize the pinned data (or reduce to 1/13 directly),
# and transport along a ring homomorphism to C. The floor-certified antecedents (O root of p,
# conj(O) = q(O)) become proved statements.
# Run:  cd ~/imsgct/ob3ect && python3 auto.py -f d12_existence_batch.yaml

name: d12-existence
out_dir: digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: 2
  temperature: 1.0
  thinking: false
  context: /home/mrnob0dy666/imsgct/d12_sic_build/d12_existence_ctx.md

entities:
  # -- re-entry: the obligation is existence alone now (supersedes v3) --
  - "the d=12 discharge obligation becomes the existence statement alone, because the norm identity and all 143 equiangularity identities are now machine-checked exact Lean theorems on the pinned data, so what remains of crystal_forces_d12_sic is constructing the twelve coordinates as exact elements of a Lean-buildable tower and proving that the overlaps of those constructed coordinates realize the pinned minimal polynomials, superseding the v3 premise that the 143 identities themselves required the compositum tower"

  # -- the coordinate tower: five magnitude covers over K16 joined with three phase fields --
  - "the coordinate tower becomes the flat quadratic stack over the real degree-16 moduli field, adjoining the square roots of the five square-class-independent moduli N0 N1 N3 N5 N9 so that the magnitude field is degree 512 over the rationals with the other seven magnitude roots expressed through their square-class products, joined with the cyclotomic phase field of the twelfth roots of unity, the D4 degree-32 phase field, and the order-32 degree-64 phase field, built one flat quadratic layer at a time inside the list-of-rationals engine that already reduced theta to the 288th power, avoiding the nested-record representation that overflows the elaborator"

  # -- the coordinates: banked closed forms entered as tower elements --
  - "each fiducial coordinate becomes the exact tower element square-root-of-N_k times u_k, with the branch of every magnitude root selected by the 1500-digit witness and every phase entered from its banked fold data, roots of unity for k in two six eight ten, the D4 quartic two-cosine fold for k in one three seven nine, and the solvable order-32 octic fold for k in five eleven, so that the constructed twelve-tuple reproduces the numeric fiducial at the deep-vanish floor before any identity is attempted"

  # -- the crux tie: constructed overlaps must land on the pinned data --
  - "each of the 143 overlaps becomes an in-tower product whose complex conjugation is the explicit tower involution fixing every magnitude root and inverting every phase, so that the overlap times its conjugate reduces in-tower to the rational one-thirteenth and the overlap's descent must land exactly on its pinned minimal polynomial of degree at most 32, converting the floor-certified antecedents of the per-overlap equiangularity theorems into proved statements about the constructed coordinates"

  # -- the cost center and its fork: stage by orbit or re-represent per overlap --
  - "the in-tower reduction becomes the genuine cost center because every overlap mixes all twelve coordinates and so is computed in the full compositum of degree in the thousands before descending to degree at most 32, and the build must either stage the reduction by Galois orbit so the 143 checks amortize into a handful of orbit representatives or find the re-representation in which each overlap is computed directly inside its own small field, with monolithic brute reduction rejected the moment it creeps instead of flooring"

  # -- the capstone: axiom to theorem along the embedding --
  - "crystal_forces_d12_sic drops from axiom to theorem by transporting the tower-verified coordinates along the ring homomorphism into the complex numbers that selects the positive real square root for each magnitude cover and the witnessed embedding for each phase and intertwines the tower involution with complex conjugation, yielding an unconditional IsSICPOVM 12 witness in the complex numbers built from the closed-form coordinates rather than from any floating-point approximation"

  # -- the standing gate --
  - "the existence admission gate becomes the threefold requirement that the constructed coordinates reproduce the 1500-digit witness to the deep-vanish floor, that every in-tower identity is decided true under native_decide in the flat engine, and that each constructed overlap's minimal polynomial equals its banked pinned polynomial exactly, rejecting any tower layer whose reduction merely creeps with precision as the signature of a coordinate placed in the wrong layer"
