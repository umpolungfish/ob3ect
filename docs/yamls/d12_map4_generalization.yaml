# ob3ect territory-map 4/4 — the GENERALIZATION / STRUCTURAL-vs-EMPIRICAL region.
# Maps the landscape BEYOND d=12: whether the flat-field discharge method extends to
# other 2^n dimensions, how it sits against the unconditional Belnap result, and the
# Hilbert-12 payoff. Anchors (do not regress): for d=2^n the Belnap multilattice
# already carries the full SIC structure UNCONDITIONALLY (orbit 4^n=d^2, join-
# equiangular, zero axioms) — that is the Grammar's claim. The wh_inner/C^d machinery
# is the EMPIRICAL SHADOW: the same fact re-encoded in Hilbert space, open in that
# frame. Discharging crystal_forces_d12_sic closes the shadow at one dimension.
# Base field real quadratic Q(sqrt((d-3)(d+1))) for d>=4; d in {2,3} degenerate.
# Run:  cd ~/imsgct/ob3ect && python3 auto.py -f d12_map4_generalization.yaml

name: d12-map-generalization
out_dir: digital

design:
  scope: maximal
  provider: openrouter
  model: google/gemini-3-flash-preview
  retries: 2
  temperature: 1.0
  thinking: false
  context: /tmp/claude-1000/-home-mrnob0dy666-imsgct/2df909c9-a04e-4a42-8433-903523348079/scratchpad/d12_ctx

entities:
  # ── the seam between unconditional structure and conditional shadow ───────
  - "the boundary between the unconditional Belnap multilattice SIC and the conditional complex-Hilbert-space SIC becomes an explicit one-way imscription, where discharging the d=12 fiducial axiom is the shadow re-absorbing one dimension of a fact the Grammar already holds"

  # ── does the flat-field method generalize across 2^n ──────────────────────
  - "the flat-field exact-fiducial discharge demonstrated at d=12 becomes a dimension-indexed method whose feasibility is governed by the ray class field degree of Q(sqrt((d-3)(d+1))), predicting which 2^n dimensions are within native_decide reach and which explode"

  # ── the Hilbert-12 payoff, made concrete ──────────────────────────────────
  - "the explicit generators of the conductor-36 ray class field of Q(sqrt13) recovered from the d=12 fiducial become a concrete instance of Hilbert's twelfth problem for a real quadratic field, an abelian extension generated by SIC coordinates rather than by transcendental special values"

  # ── the degenerate small dimensions as the boundary of the arithmetic ─────
  - "the dimensions d=2 and d=3, where the SIC discriminant (d-3)(d+1) is non-positive, become the degenerate boundary of the arithmetic regime, the tetrahedron and the continuous equilateral family that need no Stark unit and mark where the real quadratic story begins"

  # ── the general Zauner statement as the limit of the per-dimension route ──
  - "the Zauner conjecture for all 2^n becomes the direct limit of the per-dimension flat-field discharges, each dimension an independent arithmetic problem over its own real quadratic field, unified only by the single Belnap structural skeleton they all shadow"
