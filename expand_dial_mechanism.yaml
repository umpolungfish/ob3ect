# ob3ect: determine the mechanism a proof-expansion CLI degree must be.
# We built MoDoT's --expand N as an LLM targeting a line count; the open question
# is whether that is the structurally correct dial or whether it must be a
# deterministic elaboration ladder. Route the decision through a constructed
# ob3ect rather than by bias — the imscribed structure forces the answer.
#
# Run:  cd /home/mrnob0dy666/ob3ect && python3 auto.py -f expand_dial_mechanism.yaml

name: expand_dial_mechanism
out_dir: digital

design:
  scope: local
  provider: openrouter
  model: google/gemini-3-flash-preview
  domain: computational

entities:
  - "a component that renders the conventional proof (the T/F-lane Witness) of a validated ob3ect at a caller-chosen degree of detail, from the pinched minimal form to a fully walked-out form, every rendering closing the identical kernel theorem with no loss of structural fidelity."
