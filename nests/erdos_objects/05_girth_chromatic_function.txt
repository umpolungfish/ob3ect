An object that assigns to a pair (k, n) the largest girth achievable by a graph
on n vertices whose chromatic number is at least k. The object must express the
behaviour of that quantity as n grows with k fixed, and must express the
construction realising a large girth at high chromatic number.
