An object over self-avoiding walks on a lattice of given dimension that assigns
to each length the mean-square displacement of the walk's endpoint, averaged over
all walks of that length, and that expresses the exponent governing its growth.
The object must express how the exponent depends on the dimension and where the
dependence is known.
